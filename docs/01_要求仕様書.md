# エンジニア キャリアプランシート 要求仕様書

## 1. この資料について

キャリアプランシートに対する要求を一元管理する。何を求められ、それをどう扱ったかを記録する。
仕様書（02_仕様書.md）が「何を作るか」を定めるのに対し、本書は「なぜそうなっているか」を保持する。

| 章 | 内容 |
| --- | --- |
| 2 | 記載ルール |
| 3 | 目的から導出した要求 |
| 4 | 打合せから起票した要求 |
| 5 | レビューから起票した要求 |
| 6 | 保留・見送りの一覧 |

## 2. 記載ルール

要求は REQ-nnn で識別する。番号は再利用しない。

| 項目 | 内容 |
| --- | --- |
| 出所 | 目的／議事録（日付）／レビュー |
| 実現性 | 可／条件付き可／不可 |
| 状態 | 反映済／保留／見送り |
| 反映先 | 仕様書の章番号。見送りの場合は理由を書いた章 |

状態が「反映済」であっても、仕様が確定したという意味ではない。未確定の値やロジックは本書6章で管理する。

---

## 3. 目的から導出した要求

### REQ-001 自分の現在地を言語化できること

- 出所：目的
- 要求：回答者が、自分がどの分野のどの工程までできるかを言葉にできる状態にする
- 実現性：可
- 難所：単一の指標では表現できない。分野と工程の2軸が必要
- 対応方針：
  1. 業務ジャンルと工程の2軸で守備範囲を表す
  2. 目的を恩恵 B1〜B4 に分解し、分析はこの4つを満たすために行う
  3. 職種は結果に付くラベルとして扱う
- 反映先：2.2、3.1、5.2
- 状態：反映済

### REQ-002 全回答者が自分を写せること

- 出所：目的
- 要求：どの分野のエンジニアが回答しても、自分の経験を記入できるようにする
- 実現性：条件付き可
- 難所：現行シートはWeb系5ジャンルしか持たず、IT分野の網羅率が28%だった
- 対応方針：
  1. IT分野を18ジャンルに整理し、網羅率を100%にする
  2. MS分野は対象外とし、v2で扱う
- 反映先：4.2
- 状態：反映済

### REQ-003 順位や等級で評価しないこと

- 出所：目的
- 要求：総合評価、等級、適合度％、順位を出さない
- 実現性：可
- 難所：依頼元から「推奨職種を算出したい」という要望があり、衝突する
- 対応方針：職種名は出すが、順位と数値は付けない。根拠は 03_エビデンス編.md
- 反映先：2.4、5.4、5.5
- 状態：反映済

### REQ-004 自己評価の精度を担保すること

- 出所：レビュー（エビデンス調査）
- 要求：順位や比較を出さない場合でも、回答者が判断できる基準を与える
- 実現性：可
- 難所：自己評価と客観指標の相関は平均0.29と低い。規範的基準がないと精度が出ない
- 対応方針：アンカーを規範的基準として機能させる。各段の意味を外部から定義する
- 反映先：3.4
- 状態：反映済

### REQ-005 設問の粒度を揃えること

- 出所：目的
- 要求：同一カテゴリ内の設問が同じ抽象度で並ぶようにする
- 実現性：可
- 難所：現行シートは言語・ライブラリ・製品・ツールが同じ列に混在していた
- 対応方針：
  1. 設計ルール4条を定め、3ブロックに分離する
  2. 個別技術はジャンルのスロットに降ろす
- 反映先：2.5、3.2
- 状態：反映済

---

## 4. 打合せから起票した要求

出所はすべて 2026年8月4日の打合せ（docs/minutes/20260804_打合せ議事録.md）。

### REQ-101 業務ジャンルにシステムコンサルを追加

- 要求：業務分析、システム構想、全体アーキテクチャ設計を扱う「システムコンサル」をジャンルに追加する
- 実現性：不可
- 難所：ジャンルは4スロット（主要言語、主要フレームワーク、設計様式、実行環境）で測る。システムコンサルは3つが埋まらず、レーダーが成立しない
- 対応方針：
  1. ジャンルには追加しない
  2. REQ-102 と REQ-108 で意図を満たす
- 反映先：なし（見送り。理由は本項目に記載）
- 状態：見送り

### REQ-102 要件定義に業務分析系の観点を追加

- 要求：要件定義に次の3観点を追加する
  1. 現行業務の分析
  2. 現在の課題の特定
  3. システム化方針の立案
- 実現性：可
- 難所：既存観点「業務・現行システムの理解」と重なる
- 対応方針：既存観点を「現行業務・現行システムの分析」に改称して統合し、8観点に拡張する
- 反映先：4.3.1
- 状態：反映済

### REQ-103 全体アーキテクチャ設計を工程に追加

- 要求：工程に全体アーキテクチャ設計を追加する
- 実現性：可（対応不要）
- 難所：なし
- 対応方針：基本設計の観点1「システム全体構成の決定」に含まれるため追加しない
- 反映先：4.3.2
- 状態：反映済

### REQ-104 汎用能力のカテゴリ数を各グループ5件に揃える

- 要求：G1・G2・G3 のカテゴリ数を5件に揃える
- 実現性：条件付き可
- 難所：5件に揃えると2つの問題が生じる
  1. G3 に3カテゴリを新設する必要があり、枠を埋めるためのカテゴリが混じる
  2. 観点を3件に減らす必要があり、カテゴリ名に対して中身が薄くなる
- 対応方針：各4カテゴリ × 4観点に変更する。レーダーの軸は4から6の範囲であれば読めるため、5である必然性がない
- 反映先：4.4
- 状態：反映済（内容を変更）

### REQ-105 汎用能力の設問数を減らす

- 要求：汎用能力の設問が多すぎる
- 実現性：可
- 難所：カテゴリを増やしながら設問を減らす必要がある
- 対応方針：カテゴリを11から12にしつつ、観点を各4件に固定する。58問から48問に減
- 反映先：4.4、4.6
- 状態：反映済

### REQ-106 G1にカテゴリを1件追加

- 要求：G1 に入力項目を1件追加する
- 実現性：可
- 難所：なし
- 対応方針：
  1. G1-4 データ操作・永続化 を新設する
  2. コード理解とデバッグを G1-2 に統合し、4カテゴリに整理する
- 反映先：4.4.1
- 状態：反映済

### REQ-107 G3にカテゴリを3件追加

- 要求：G3 に入力項目を3件追加する
- 実現性：条件付き可
- 難所：3件追加すると5カテゴリになり、REQ-104 の判断と衝突する
- 対応方針：
  1. G3-3 顧客・ステークホルダー対応、G3-4 セルフマネジメントの2件を新設する
  2. 育成は G3-2 の観点に含める
- 反映先：4.4.3
- 状態：反映済（件数を変更）

### REQ-108 守備範囲にシステムコンサルを追加

- 要求：守備範囲にシステムコンサルを追加する
- 実現性：条件付き可
- 難所：守備範囲マップの縦軸は業務ジャンルであり、職種ではない
- 対応方針：対応可能なポジションの判定条件に「システムコンサルタント」を追加する
- 反映先：5.5
- 状態：反映済（実現手段を変更）

### REQ-109 レーダーをG1・G2・G3の3枚にする

- 要求：レーダーをG1・G2・G3の粒度で表示し、各5項目の軸にする
- 実現性：条件付き可
- 難所：軸数は REQ-104 の結論に依存する
- 対応方針：
  1. レーダーを3枚にする
  2. 各レーダーの軸は4項目とする
- 反映先：5.3
- 状態：反映済（軸数を変更）

### REQ-110 強みの別枠表示を廃止

- 要求：関連職種とポジションに理由を箇条書きで記載するため、強みの別枠は不要
- 実現性：可
- 難所：職種にもポジションにも紐づかない高評価の観点が出力から消える懸念があった
- 対応方針：廃止する。汎用能力のレーダー3枚が全カテゴリを表示し、守備範囲マップのバーが重み付けを表示するため、情報は失われない
- 反映先：7.2
- 状態：反映済

### REQ-111 「伸びしろ」の改称と抽出条件の定義

- 要求：「伸びしろ」を「課題」や「改善内容」に改称し、未回答を除いて評価値が低いものを抽出する
- 実現性：可
- 難所：未回答と未経験を区別する必要があるかが不明確だった
- 対応方針：
  1. 未回答は未経験と同じ扱いとし、値1とする
  2. 課題は値2と3を対象とする
  3. 値1のうち守備範囲に接するものは隣接として別に表示する
- 反映先：5.1、5.6
- 状態：反映済

### REQ-112 関連職種を業務ジャンルの重み付けから算出

- 要求：業務ジャンルの重み付けをもとに、該当する職種を算出する
- 実現性：条件付き可
- 難所：ジャンルと職種の対応表が存在しない
- 対応方針：
  1. 18ジャンルそれぞれに職種名を割り当てた対応表を作成する
  2. 表示件数は sample の出力を見てから決める。閾値の採用根拠はREQ-222にある
- 反映先：4.2、5.4
- 状態：反映済（閾値の採用根拠はREQ-222を参照）

### REQ-113 対応可能なポジションを工程の重み付けから算出

- 要求：工程の重み付けをもとに、プロジェクトにおける役割が分かる文言で表示する
- 実現性：条件付き可
- 難所：
  1. 判定条件が存在しない
  2. 工程だけでは判定できないポジションがある
- 対応方針：
  1. 8ポジションの判定条件を暫定で定義する
  2. 一部は汎用能力のカテゴリを条件に含める
  3. 展開後に調整する
- 反映先：5.5
- 状態：反映済（条件は暫定）

### REQ-114 入力結果をJSONで出力

- 要求：入力結果をJSONで出力可能にする
- 実現性：可
- 難所：設問名をキーにすると、設問名の変更で過去データが読めなくなる
- 対応方針：表示名とは独立した固定IDを付与し、それをキーにする
- 反映先：3.5、6.1
- 状態：反映済

### REQ-115 JSONを読み込んで過去の入力と分析結果を表示

- 要求：出力した結果を読み込むことで、過去の入力内容と分析結果を表示できるようにする
- 実現性：可
- 難所：分析結果を保存すると、ロジック変更後に過去データを再評価できない
- 対応方針：分析結果は保存しない。入力値のみを保存し、読み込み時点のロジックで再計算する
- 反映先：6.1、6.2、6.3
- 状態：反映済

---

## 5. レビューから起票した要求

### REQ-201 工程「実装」の観点を成立させる

- 出所：レビュー
- 要求：工程「実装」の観点が評価として機能していない
- 実現性：可
- 難所：実装の行為は「コードを書く」ほぼ一つで、他の観点は汎用能力と重複する
- 対応方針：
  1. 実装対象の種類（新規機能、既存改修、共通部品）で分ける
  2. 「コーディング（規約準拠）」は全員が4以上になり差がつかないため廃止する
- 反映先：4.3.4
- 状態：反映済

### REQ-202 守備範囲マップで工程の習熟度が読めるようにする

- 出所：レビュー
- 要求：工程ができていても、回答済みジャンルとの交点しか塗られず、できていないように見える
- 実現性：可
- 難所：工程を1回しか聞かないため、マップのセルは行と列の情報の積でしかない
- 対応方針：
  1. 有効な工程は列全体、有効なジャンルは行全体に色を付ける
  2. 習熟度をバーで併記する
- 反映先：5.2
- 状態：反映済

### REQ-203 ジャンルのグループ単位の近さを表現する

- 出所：レビュー
- 要求：ジャンルに一列の順序はないが、Web系のようにグループ単位の近さは存在する
- 実現性：可
- 難所：二面性を持つジャンルは、排他的な分割ではどちらかの近さを失う
- 対応方針：6グループの分割を仮置きする。運用データの共起で再編する
- 反映先：4.2
- 状態：反映済（グループは仮置き）。REQ-240 でグループの役割を表の見出しに限定した。ジャンルどうしの近さはグループではなくジャンルの関連表が表す。本REQの要求は、グループではなく関連表が満たしている

### REQ-204 関連職種の表示件数上限

- 出所：レビュー（要件確定 2026-08-09）
- 要求：旧仕様書8.1 #1、関連職種の表示件数の上限を決める
- 実現性：可
- 難所：なし
- 対応方針：
  1. 上限なしとする
  2. 閾値3.5以上に該当したジャンルをすべて表示する（3.5自体の採用根拠はREQ-222にある）
  3. 表示順はジャンル定義順とし、値による並べ替えは行わない
- 反映先：5.4
- 状態：反映済

### REQ-205 課題・改善内容の表示件数上限

- 出所：レビュー（要件確定 2026-08-09）
- 要求：旧仕様書8.1 #2、課題・改善内容の表示件数の上限を決める
- 実現性：可
- 難所：なし
- 対応方針：
  1. 上限なしとする
  2. 値2または3に該当する観点をすべて表示する
- 反映先：5.6
- 状態：反映済

### REQ-206 ポジション名・職種対応表の呼び方

- 出所：レビュー（要件確定 2026-08-09）
- 要求：旧仕様書8.1 #5、5.5節ポジション名・4.2節の職種名の呼び方を確定する
- 実現性：可
- 難所：なし
- 対応方針：現行案のまま確定する。表示ラベルであり値の算出ロジックとは独立しているため、実務の呼称と異なる指摘があれば表記のみ更新する
- 反映先：4.2、5.5
- 状態：反映済

### REQ-207 天井効果が出た観点の書き換え基準

- 出所：レビュー（要件確定 2026-08-09）
- 要求：旧仕様書8.1 #6、天井効果が出た観点の書き換えの判断基準を決める
- 実現性：可
- 難所：実際にどの観点が天井効果に該当するかは回答データがないと分からない
- 対応方針：
  1. 値そのものは確定せず、見直しトリガーを確定する
  2. トリガー条件は、回答20件以上、かつある観点で値4・5の回答が80%を超えたら、識別力を上げる書き換えを検討する
  3. 20件・80%は暫定値であり、運用開始後の最初の見直しで調整してよい
- 反映先：なし（運用ルールとして本項目に記載）
- 状態：反映済

### REQ-208 迷いの多かった観点へのアンカー文言追加基準

- 出所：レビュー（要件確定 2026-08-09）
- 要求：旧仕様書8.1 #7、アンカー文言追加の判断基準を決める
- 実現性：可
- 難所：回答者が観点の意味に迷ったことを申告する経路がシステムにない
- 対応方針：
  1. システムに申告機能は追加しない（原則8「選択という操作を設けない」の趣旨に合わせ、入力側を増やさない）
  2. 運用ルールとして確定：面談等で拾った「わかりにくい」という声が同一観点について3件以上集まったら追記を検討する
  3. 3件は暫定値であり、運用開始後に調整してよい
- 反映先：なし（運用ルールとして本項目に記載）
- 状態：反映済

---

## 6. 保留・見送り

| ID | 要求 | 状態 | 理由の記載場所 |
| --- | --- | --- | --- |
| REQ-101 | 業務ジャンルにシステムコンサルを追加 | 見送り（v2で再検討） | 本書 REQ-101 |
| REQ-213 | prototypeにG3-2を追加する | 見送り（sampleをv1で凍結する方針のため対象外） | — |
| REQ-231 | 設問のIDの付け方を決める | 保留（設問確定後） | 本書 REQ-231 |
| REQ-239 | 上位職の判定条件を現行構成で建て直す（REQ-216・REQ-214を統合） | 保留（構造は反映済。数値と土台の割り当ては sample の出力で確認して決める） | 本書 REQ-239 |
| REQ-238 | ブロック2を経験の有無だけを聞くものにする | 保留（ブロック2の曖昧性確認をこのREQの結論待ちで中断中） | 本書 REQ-238 |
| REQ-243 | ジャンル表の職種名に役割の名前が混じっている | 反映済（#16を業務システム導入エンジニアに変更） | 本書 REQ-243 |

v2以降の検討候補、および sample の出力を見ないと決められない項目は、本書の該当REQに記載する。仕様書（02）には反映しない。

「sample の出力を見て決める」とは、prototype/sample_v2.html に値を入れて動かし、その結果を見て判断することを指す。複数人から回答を集めることではない。

対応可能なポジションの判定条件（5.5節）は REQ-239 で一括管理する。数値と土台の割り当ては本書に案として置き、sample の出力で調整して承認を得てから仕様書に入れる。関連職種の閾値（5.4節）は対象が異なるため REQ-222 として分離し、こちらは確定済みである。

### REQ-209 関連職種の算出をB2に合わせる

- 出所：レビュー（2026-08-09）
- 要求：関連職種は「関連しているもの」を出す以上、すでに該当している職種ではなく、近いがまだ届いていない職種を出すべき
- 実現性：可
- 難所：従来の算出条件（重み付け3.5以上）は、本人がすでに知っている職種しか返さない
- 対応方針：
  1. 同グループ内に3.5以上のジャンルがあり、自身が3.5未満のジャンルの職種を出す
  2. すでに該当している範囲は対応可能なポジション（B1）が担う
- 反映先：2.2、5.4
- 状態：反映済（判定値3.5の採用根拠はREQ-222を参照）。対応方針は REQ-240 で2点を見直した。1はグループではなくジャンルの関連表を根拠にする形に変え、2は前提が成立していなかったため取り消した。5.5 は工程から算出するポジションであり、ジャンル側の職種名を受け取っていない。到達済みのジャンルの職種名は 5.4 が出す

### REQ-210 5.5節ポジションの切り分けを再設計する

REQ-212に統合。単独では扱わない。

### REQ-211 上位職にプロダクトマネージャーを追加する

REQ-212に統合。単独では扱わない。

### REQ-212 5.5節ポジションの再設計とプロダクトマネージャーの扱い

- 出所：レビュー（2026-08-11、REQ-210・REQ-211を統合）
- 要求：5.5節の対応可能なポジションの切り分けが良くない。ポジションの割り当てが分析の目的（B1：経験範囲を人に説明できる）に沿うよう見直す。現行8件は次のとおり
  1. プログラマー
  2. テスター・QA
  3. 設計担当
  4. 要件定義担当
  5. システムコンサルタント
  6. テックリード
  7. プロジェクトリーダー
  8. 運用担当
- 実現性：条件付き可
- 難所：現行の判定条件を精査すると3点の問題がある
  1. プロジェクトリーダーの条件がG3-2単独で工程条件を持たず、5.5節自身の前提（工程の重み付けをもとに算出する）と矛盾する
  2. システムコンサルタント該当者は数値上必ず要件定義担当にも該当し、テックリード該当者は数値上必ずプログラマーにも該当するが、この包含関係が意図的な設計として明示されていない
  3. 「要件定義担当」「設計担当」という呼称が実務の呼称と一致しない。厚労省job tagは要件定義〜設計を「システムエンジニア」という1つの役割として扱う
- 対応方針：ポジションを性質の異なる2種類に分けて再構成する
  1. 単一的ポジション（工程1つまたは隣接工程に独立して到達していることを示す基礎ポジション、4件）：システムエンジニア（要件定義〜設計）、プログラマー（実装）、テスター・QA（テスト）、運用担当（運用・保守）
  2. 上位職（単一的ポジションの条件を包含し汎用能力カテゴリの条件を上乗せして初めて成立する応用ポジション、5件）：システムコンサルタント、テックリード、アーキテクト、プロジェクトリーダー、プロジェクトマネージャー
  3. カタログの選定基準（4点）：実在性・現在性（実務で現在も使われているか）、対象者適合（2.3節の対象者が名乗りうるか）、重複の整理（他候補と概念が大きく重ならないか）、手法非依存（特定の開発手法の中でのみ定義されるアカウンタビリティでないか）
  4. 上記基準により不採用とした候補：CTO（経営層で対象者に不適合）、SRE（4.2節のジャンル定義表に既出で重複）、エンジニアリングマネージャー（公式な定義主体がなくプロジェクトマネージャーと重複）、プロダクトオーナー（Scrum Guide 2020版が「役割」を「アカウンタビリティ」に呼称変更しているとおりScrum固有の責務であり、4.4.2節がウォーターフォールとアジャイル・スクラムを並列に扱う本シートの手法非依存の設計と矛盾する）
  5. 各上位職が土台にする単一的ポジションは5.5節に反映した
  6. 個々の判定条件の具体的な数値は依頼者と合意しておらず、REQ-216〜221として別途切り出す
  7. プロダクトマネージャーの扱いはREQ-214・REQ-215に切り出す
- 反映先：5.5
- 状態：反映済（カタログと2階層構造はv1.3の本文に反映済み。判定条件の数値は未承認でREQ-216〜221に切り出した）

### REQ-213 prototypeにG3-2を追加する（REQ-212の前提）

- 出所：レビュー（2026-08-12、REQ-212の実装作業中に判明）
- 要求：REQ-212で新設した上位職のうちテックリード・プロジェクトリーダー・プロジェクトマネージャーはG3-2（チーム運営・リーダーシップ）の値を判定条件に使うが、prototype/sample_v1.htmlのGENERAL_SAMPLESにはG1-1・G2-2・G3-3しかなく、G3-2が存在しない
- 実現性：可
- 難所：なし
- 対応方針：
  1. sample_v1.htmlは今回のバージョンでは更新しない（v1のまま凍結する）
  2. 将来sampleを更新する時点で再検討する
- 反映先：なし
- 状態：見送り（sampleをv1で凍結する方針のため対象外）

### REQ-214 上位職にプロダクトマネージャーを追加する

- 出所：レビュー（2026-08-12、REQ-212から分離）
- 要求：上位職にプロダクトマネージャーを追加する
- 実現性：条件付き可
- 難所：実在性・対象者適合・手法非依存のいずれも満たすが、判定の根拠となる観点が現行の設問セット（4.3工程・4.4汎用能力）に存在しない。最も近い候補である次の3つも、いずれもプロダクトマネジメントの核心（事業・ユーザー価値に基づく優先順位判断を継続的に行う力）を直接測っておらず、既に他のポジションの根拠として使われている観点の転用は二重取りになるため使えない
  1. 要件定義の観点6「スコープと優先度の判断」
  2. G3-3「顧客・ステークホルダー対応」
  3. G2-2「アジャイル・スクラムの進め方」
- 対応方針：REQ-215（新規観点の追加）が解消してから追加する
  1. 同一バージョンで対応するという当初の指定は、REQ-215を先に反映する形に改める。2026-08-13
  2. 当初の指定の意図は、判定の根拠となる観点を持たないポジションが仕様書に残るのを防ぐことにあった。順序はポジションが先に入る向きだけを禁じており、観点が先に入る向きは意図に反しない
  3. REQ-215はREQ-227の再設計と不可分になったため、同一バージョンを守ると本REQの判定条件までREQ-227に巻き込むことになる。判定条件の数値はREQ-216と同じ性質の判断であり、そちらに合流させる
- アクションアイテム：
  1. 確定した観点をもとにプロダクトマネージャーの判定条件を定義する
  2. 上位職カタログにプロダクトマネージャーを追加し、5.5節に反映する
- 反映先：5.5（REQ-239と同時）
- 状態：REQ-239 に統合（2026-08-15）。前提の REQ-215 は反映済で、残るのは判定条件のみだった。難所に挙げた「判定の根拠となる観点が存在しない」は、REQ-215 が業務理解と価値判断の設問2・3 を追加したことで解消している

### REQ-215 プロダクトマネージャー判定のための新規観点を追加する（REQ-214の前提）

- 出所：レビュー（2026-08-12、REQ-214の前提）
- 要求：プロダクトマネージャーの判定に使う、事業・ユーザー価値に基づく優先順位判断を測る新規の観点を追加する
- 実現性：条件付き可
- 難所：
  1. 新規観点をG3に追加するか要件定義工程に追加するかが未定
  2. 追加した場合の設問数増加（4.4節は12カテゴリ×4観点＝48問で確定済み）をどう扱うかも未定
- 対応方針：承認日 2026-08-15。難所1・難所2とも解消した
  1. 置き場所はブロック3の「業務理解と価値判断」とする。2026-08-15。節番号は REQ-234 で 4.4.6 から 4.4.5 に繰り上がった
  2. 要件定義工程に追加する案は採らない。工程は作ると決まったものを前提に始まるため、何を作るかを決める力は工程の観点をいくら細かくしても出てこない
  3. 本REQはREQ-227と同時に仕様書へ反映する。単独では反映しない
  4. アンカー型はブロック3の型に従う。REQ-230でアンカーを習熟度型と関与型の2つに整理し、割り当てをブロック単位にしたため、ブロック3は習熟度型となる。2026-08-13に「A 自立度型」としていた記述はこれに置き換わる
  5. 測る中身は、価値、優先度、効果測定とする。やらないことの決定は独立した設問にしない。2026-08-15
  6. 難所2は前提ごと消滅した。「12カテゴリ×4観点＝48問」の枠はREQ-229で固着化記述として削除しており、収める枠が存在しない
- アクションアイテム：
  1. 完了。設問数の扱いは決める必要がなくなった。理由は対応方針6。2026-08-15
- 反映先：4.4.5 業務理解と価値判断（設問2「事業・ユーザー価値に基づく優先順位づけ」、設問3「提供後の効果測定と方針の見直し」）
- 状態：反映済（承認日 2026-08-15。仕様書 v1.5）

### REQ-216 5.5節の判定条件の数値が未承認

REQ-217〜221を統合。**2026-08-15 に廃止し、REQ-239 に建て替えた。**要求と対応方針が廃止済みID（G1-1、G3-2、G3-3）と旧設問セットの上に組まれており、参照先を1件ずつ差し替えるより現行構成で組み直すほうが早いため。以下は経緯の記録であり、現在の正は REQ-239 にある。

- 出所：レビュー（2026-08-12、5.5節の記載内容を精査して判明）
- 要求：5.5節の判定条件の数値は、いずれも依頼者と合意していない。対象は次の6件
  1. システムエンジニア（単一的ポジション）：要件定義3以上、または（基本設計3以上 かつ 詳細設計3以上）
  2. システムコンサルタント（上位職）：要件定義4以上 かつ G3-3 3以上
  3. アーキテクト（上位職）：基本設計4以上 かつ 詳細設計4以上 かつ G1-1 3以上
  4. テックリード（上位職）：実装4以上 かつ 詳細設計4以上 かつ G3-2 3以上（旧仕様から継続する数値で、旧仕様書8.1節#4で以前から依頼者確認中とされていたもの）
  5. プロジェクトリーダー（上位職）：実装3以上 かつ G3-2 4以上
  6. プロジェクトマネージャー（上位職）：実装3以上 かつ 運用・保守3以上 かつ G3-2 4以上
- 実現性：可
- 難所：精査の結果、要求に挙げた6件のほかに3点が判明した
  1. 単一的ポジション4件のうち、プログラマー・テスター・QA・運用担当の判定条件がどこにも記載されていない
  2. プロジェクトマネージャーの条件がプロジェクトリーダーの条件を完全に含み、差分が運用・保守3以上だけになっている
  3. アーキテクトの「詳細設計4以上」と「G1-1 3以上」が近い対象を測っている。この重複は4.5節に登録がない。REQ-226で扱う
  4. 汎用能力側の条件をどう書くかが決まっていない。2026-08-15に判明。選択肢は3つある。(a) カテゴリの平均を使う（旧方式。「設計力3以上」）。1問低くても他で補えるが、結果画面に出る根拠が読んで分からず、カテゴリの区切りを変えると判定も変わる。(b) 設問を直接指定する（「オブジェクト指向・モジュール設計3以上」）。根拠が読んで分かりB1に効くが、1問で決まるため条件が厳しい。(c) カテゴリ内の複数設問を指定する（「設計力の4問のうち3問以上が3以上」）。中間だが条件式が複雑になる。方式だけ先に決めても、6上位職ぶんの条件を実際に書くと変わる可能性があるため、数値と一緒に扱う
  5. 上記の要求と対応方針が参照している G1-1・G3-2・G3-3 は、REQ-227 の再設計で廃止された。参照先を新しい設問に置き換える必要がある
- 対応方針：一部確定。承認日 2026-08-13
  1. 単一的ポジション4件はすべて工程3以上で揃える。システムエンジニアは要件定義3以上または（基本設計3以上かつ詳細設計3以上）、プログラマーは実装3以上、テスター・QAはテスト3以上、運用担当は運用・保守3以上。分析の目的がB1「経験範囲を人に説明できる」であり、即戦力の判定ではないため
  2. 上位職には土台にする単一的ポジションの名前を併記し、数値条件は上位職側だけを書く。上位職の数値が土台の数値を数学的に含むため、二重に書かない
  3. プロジェクトマネージャーは要件定義3以上 かつ G3-2 4以上 かつ G3-3 3以上とする。土台はシステムエンジニア。運用・保守の条件は外す。厚労省job tagがプロジェクトマネージャ（IT）の仕事を「顧客への報告や関係者間の意見調整」とし、就業前の経歴をシステムエンジニアとしているため。運用・保守は職務ではなく経歴として現れる
  4. アーキテクトは現行のまま、基本設計4以上 かつ 詳細設計4以上 かつ G1-1 3以上とする。REQ-226の整理結果を見てから見直す
  5. G3-2の要求水準がテックリード3以上、プロジェクトリーダー・プロジェクトマネージャー4以上と混在することは残す。技術面の主導とチーム運営の責任を区別するため。理由を5.5節に書く
- アクションアイテム：
  1. 汎用能力側の条件の書き方（難所4の a／b／c）を決める。数値と同時に扱う
  2. 参照先を新しい設問に置き換える（難所5）
  3. システムコンサルタント、テックリード、プロジェクトリーダーの数値を依頼者に提示し、承認を得る
  4. 承認された数値を5.5節に反映する
- 反映先：5.5
- 状態：廃止（2026-08-15。REQ-239 に建て替え）。対応方針1〜3 の判断はIDに依存しないため REQ-239 が引き継いだ。対応方針4・5 と難所4・5 は廃止済みIDの上に組まれていたため引き継がない

### REQ-222 関連職種の閾値3.5の数値確認

- 出所：レビュー（2026-08-13、REQ-112・204・209で閾値3.5が確定値のように扱われていたため切り出し）
- 要求：5.4節の関連職種の算出条件で使う閾値3.5（同グループ内のジャンル判定・自身のジャンル判定の両方に使用）は、依頼者と合意していない
- 実現性：可
- 難所：REQ-204・REQ-209の対応方針が閾値3.5を前提として記載しているため、内容としては確定済みに見えるが、値そのものは未承認
- 対応方針：承認日 2026-08-13
  1. 閾値は3.5とする
  2. 採用の根拠は、ジャンルの重み付けがS1〜S4の平均であり、全スロットが3以下では平均が3.0を超えないため、平均3.5以上が値4以上のスロットを最低1つ持つ点にある。これは5.1節の「値4〜5を関連職種および対応可能なポジションの根拠として使う」と一致する
  3. 上記2の性質は3.0を超える閾値であれば成立するため、3.5である必然性までは示していない。4スロットが整数値であることから、同じ性質を持つ最小の閾値は3.25（合計13）である。3.5と3.25の差は、値4以上を1つ持つジャンル（4,3,3,3）を対象に含めるかどうかに現れる。3.5を採る積極的な理由は未定とし、sample の出力を見て再検討する余地を残す
  4. 根拠側のジャンル判定と自身のジャンル判定には、同じ値3.5を使う。同値であれば各ジャンルが根拠側と候補側のどちらか一方に必ず入り、隙間も重なりも生じない
  5. 上記2の根拠を仕様書5.4節に残す。依頼者の指示による
- アクションアイテム：
  1. 上記3の「3.5を採る積極的な理由」を、sample の出力を確認した時点で再検討する
- 反映先：5.4
- 状態：反映済（承認日 2026-08-13）。REQ-240 で根拠側の範囲がグループからジャンルの関連表に変わったが、閾値3.5と対応方針4 の同値の性質はそのまま成立する。根拠側は守備範囲の職種（3.5以上）、候補側は関連職種（3.5未満）になった

### REQ-223 汎用能力の観点から除外した項目の記録

- 出所：文書整理（2026-08-13、削除した旧仕様書9.2相当の記録を本書に移設）
- 要求：該当なし。観点を絞り込む過程で不採用とした項目と理由が、旧仕様書の付録削除に伴いどこにも残っていなかったため、本書に記録する
- 実現性：可
- 難所：なし
- 対応方針：以下の項目を不採用としたことを記録する。復活させるかはv2で検討する
  1. 非同期・並行処理（ジャンル依存が強い）
  2. メモリ・リソース管理（ジャンル依存が強い）
  3. 外部コード・ライブラリの挙動確認（G1-2の構造把握に含まれる）
  4. 知見の文書化と展開（G3-1のレビューでの指摘に含まれる）
  5. 監視・オブザーバビリティ（工程「運用・保守」に一本化した）
  6. ログ・トレースの設計と活用（G2-2の監視と重複）
  7. プロファイリング・性能分析（頻度が低い）
  8. 静的解析・規約の運用（G2-1のツールチェーンに寄せた）
  9. 可読性・命名（G1-3のリファクタリングに含まれる）
- 追記（2026-08-14、REQ-227の再設計）：上記1・2・7を復活させ、仕様書4.4の項目5「性能とリソースの扱い」の中身にした。不採用の理由「ジャンル依存が強い」は、観点を分野の文脈で書いていたことによる。分野名を書かずに処理性能・メモリ・並行処理として書けば、どの分野でも成立する。「頻度が低い」については、未経験を値1として受け取る設計であり、低い値が並ぶこと自体は情報として意味を持つため、不採用の理由にしない。残る6件はv2で検討する
- 反映先：なし（記録のみ。v2検討時の参考資料）
- 状態：反映済（2026-08-13にREQ-227の結果を追記）

### REQ-224 未確定事項の置き場所を要求仕様書に一本化する

- 出所：文書整理（2026-08-13、反復手順の導入時に判明）
- 要求：仕様書1章の章立て表が「8 未確定事項」「9 付録」を載せているが、本文は7章で終わっており両章は存在しない。未確定事項は本書6章に移っているため、参照を実態に合わせる
- 実現性：可
- 難所：なし
- 対応方針：
  1. 仕様書1章の章立て表から8章・9章の行を削除し、未確定事項を本書6章で管理する旨を明記する
  2. 仕様書は未確定事項を持たない。確定していない値やロジックは本書6章の該当REQに置き、仕様書本文には暫定値とREQ番号の参照だけを書く
  3. 残った要求を仕様書に反映する順序を保持する文書として docs/10_作業計画.md を新設する
- 反映先：仕様書1章
- 状態：反映済（承認日 2026-08-13）

### REQ-225 現在の仕様をベースにHTMLを作り直す

- 出所：レビュー（2026-08-13、バッチ1の締めでprototypeの扱いを確認した際に決定）
- 要求：保留中の要求をすべて仕様書に反映し終えた時点で、現在の仕様をベースにHTMLを作り直す
- 実現性：可
- 難所：現行のprototype/sample_v1.htmlはv1のまま凍結しており、仕様書との乖離が2点ある。1つは関連職種の算出が工程の重み付けから職種名を引く形で、5.4節のジャンルの重み付けを使う形と異なること。もう1つは汎用能力のサンプルデータがG1-1・G2-2・G3-3の3カテゴリしかなく、4.4節の12カテゴリに足りないこと
- 対応方針：承認日 2026-08-15
  1. 既存のsample_v1.htmlは改修せず、別のファイル prototype/sample_v2.html として作り直す
  2. 着手時期の条件（保留中の要求をすべて反映し終えた後）を取り消し、REQ-239 と REQ-238 を残したまま着手する。この2件はどちらも動かした出力を見ないと決められず、出力を見るにはHTMLが要る。順序を守ると相互に待ち合って進まなくなる
  3. REQ-213の対応方針2「将来sampleを更新する時点で再検討する」が指す時点は、本REQの着手時とする
  4. 収録範囲は全設問とする。115問（ブロック1 18ジャンル×4スロット、ブロック2 24観点、ブロック3 19問）。用途は依頼者に見せるデモだが、値を入れて動かし、REQ-239 の数値調整と REQ-238 の判断に使う出力を得る道具を兼ねるため、抜粋では役に立たない
  5. 5.5 対応可能なポジションは判定条件が未定のため出力せず、未定である旨と、調整の材料になる工程の重み付け・カテゴリの平均を表示する
  6. 外部CDNに依存しない。レーダーチャートはインラインSVGで描く。単体のファイルで動作させ、配布と検証を容易にする
  7. 設問IDは 6.1 のJSON例に倣った可読な文字列を暫定で使う。REQ-231 で確定したら差し替える
- アクションアイテム：
  1. 完了。prototype/sample_v2.html を作成し、Chromeで描画を確認した。2026-08-15
  2. sample_v2.html に値を入れて動かし、出力を見て REQ-239 の数値を調整する
- 反映先：prototype/sample_v2.html。仕様書への反映はなし
- 状態：反映済（承認日 2026-08-15）。実行して判明した仕様の欠陥2件は REQ-240・REQ-241 に切り出した

### REQ-226 工程能力と汎用能力の重複を整理する

- 出所：レビュー（2026-08-13、REQ-216の判定条件を精査する過程で判明）
- 要求：ブロック2の工程能力とブロック3の汎用能力が近い対象を測っている箇所が多く、4.5節の意図的な重複の表に載っていないものがある。棚卸しして、登録するか観点を整理するかを決める
- 実現性：条件付き可
- 難所：
  1. 工程能力の全38観点のうち15観点が汎用能力と対応するが、4.5節に登録されているのは7件である
  2. 最も重複が濃いのは詳細設計で、6観点中4観点がG1-1と対応する。この組み合わせは4.5節に1行も載っていない
  3. 自己評価では回答者が「実施した行為」と「ジャンルを横断して使える力」を切り分けられず、似た設問に似た値を付ける。両ブロックの相関が上がると、5.5節の「工程 かつ 汎用能力」の条件が実質1条件になる
  4. 観点を整理すると4.6設問数、5.3レーダーの軸、4.5重複表、5.5判定条件が同時に動く
- 対応方針：一部確定。承認日 2026-08-13
  1. 未登録の重複は8件。うち4件が詳細設計×G1-1、1件が基本設計×G1-4、2件が工程×G1-2、1件が運用・保守×G3-1
  2. 詳細設計×G1-1 以外の4件を4.5節に登録する案は撤回した。下記7に置き換わる。撤回の理由は、重複を登録して認める方向が依頼者の意図と逆だったためである
  3. 転用度を測る尺度は採用しない。本人の能力を測るうえで不適切なため。ブロック3のアンカーはブロック1・2と同じ自立度型を維持する
  4. 4.4節の目的「ジャンルにも工程にも紐づかない力」は維持する。ブロック3は可能な限り工程に関係のない内容で書き上げる方針とする。4.5節は望ましくないが現時点で残っている重複の一覧として位置づけ、可能な限り減らす
  5. 切り分けのラインは4.3節の既存ルール「設問文は行為で書く。能力の表現はブロック3が持つ」に置く。ブロック2の38観点を精査した結果、これを満たしていないのは詳細設計の4観点と基本設計の1観点、計5観点だけである
  6. 詳細設計の4観点（モジュール・クラス構成の決定、内部インタフェース・データ構造の決定、処理フロー・アルゴリズムの決定、例外・エラー処理方式の決定）を、担当行為の粒度で2観点にまとめる。詳細設計は6観点から4観点になる。2026-08-14に取り消し、6観点に戻した。単独で承認を得た変更ではなく、REQ-227と同じバッチで差し戻されたため
  7. 未登録の重複を4.5節に登録する案は採らない。4.5節に登録済みの7件を含め、重複そのものを減らす方向とする。この作業はREQ-227で扱う
- 反映先：4.3、4.4、4.5
- 状態：反映済（承認日 2026-08-15。REQ-227 の再設計で目的を達成した。ブロック2から能力語を含む観点を外し、ブロック3との重複を4.5の表に列挙しない形にした。仕様書 v1.5）

### REQ-227 ブロック2とブロック3の役割分担を再設計する

- 出所：レビュー（2026-08-13、REQ-226の精査中に、重複を登録する方向と依頼者の意図がずれていたことが判明）
- 要求：ブロック2とブロック3が同じ内容を測っている箇所を減らし、それぞれのブロックが何を測るのかを明確にする。2026-08-14に依頼者が示した条件は3つ。(1) ブロック3は可能な限り汎用的な力を対象にする。(2) 回答者が何を聞かれているか分かる具体度を保つ。オブジェクト指向の習熟を聞くような、技術的に具体的な設問とする。(3) 目的と運用ルールは現行のままでよい
- 実現性：条件付き可
- 難所：
  1. 重複度の高いものから順に、見積り（工程要件定義7とG2-3-3が文言まで一致）、ヒアリング・合意形成（工程要件定義1・8とG3-3-1・2がほぼ一致）、内部構造の設計（工程詳細設計1とG1-1）、単体テスト（工程実装4とG1-3-2）である
  2. テスト自動化の行は、G1-3の4観点のいずれもテスト自動化ではなく、登録の根拠が見当たらない
  3. G1-4は4観点すべてがジャンル10に対応しており、工程ではなくジャンルに紐づいている
  4. 観点を動かすと4.3、4.4、4.5、4.6、5.2、5.3、5.5、6.1が連動する
  5. 5.5節の判定条件（REQ-216）は工程と汎用能力を「かつ」で結んでおり、重複が残ると条件が実質1つになる
  6. 観点のIDは `process.detail-design.3` のように位置番号であり、観点を増減すると番号の意味がずれる。過去のJSONを読み込むと値が別の観点に付け替わり、6.3節のスキーマ互換性の規則では検出できない
  7. 抽象度を上げて重複を消す方向は、回答できない設問を生む。2026-08-14の差戻しで確認した。「構造の設計」「書いて残す」まで抽象化すると、回答者が何を答えればよいか分からない。重複の解消と設問の具体性はトレードオフの関係にあり、具体性の側を優先する
- 対応方針：未定。精査からやり直す。2026-08-14 に組み立てた案は依頼者の差戻しにより全面撤回した（撤回した案8）。承認済みとして残るのは1件だけである
  1. 「汎用能力の観点はジャンル中立に書く」を「分野名・製品名・分野固有の設計様式を書かない。技術名は書いてよい」に改める。承認日 2026-08-13。ジャンル中立という表現は、ブロック1との重複回避と技術名の禁止を区別できていなかった。仕様書2.5 設計ルール2 に反映済み
  2. 対象範囲はブロック2とブロック3とする。ブロック1は触らない
  3. 案より先に、設問の具体度を落とさずに重複を減らすためのルールを固める。難所7による
  4. 設問を残すかどうかの判定条件を定めた。2026-08-15。ブロック2は次の3つで判定する。(1) その工程でしか出てこない業務か。(2) 同じ工程を担当した2人の間で値が割れるか。全員が必ずやることは聞かない。(3) 担当者が業務名として認識できるか。(2) は REQ-201 で「コーディング（規約準拠）」を廃止したときと同じ物差しである
  5. 上記に加えて、ブロック2はジャンルに依存しない業務だけを置く。画面設計・帳票設計・バッチ設計のような成果物名で割ると、その成果物を持たない分野の回答者の工程値が不当に低く出る。守備範囲がジャンル×工程のマスであるため、工程側にジャンル依存を持ち込めない
  6. 設問文に深さの語（主導、標準の策定、指導）を書かないというルールは、2026-08-15 に撤回した。アンカーAの値5が「標準を定め、他者を指導できる」と特定の行為を名指ししていたために入れ子が起きていたのであり、原因は設問ではなくアンカーの側にあった。汎用の物差しであれば「チーム標準の策定」に「人に教えられる」を当てても成立する。アンカーの整理は REQ-230 で行う。このルールで削除した設問（技術的な意思決定の主導、チーム標準の策定、若手の指導・オンボーディング、ブロック2の設計標準・規約の策定）は復活の対象とする
  7. ブロック3のカテゴリの粒度を定義した。1カテゴリはエンジニアが価値を出す仕方の1種類とする。同じカテゴリの設問は同じ場面で同時に発揮され、違うカテゴリの設問は得意な人が別々になりうる。設計が得意な人とデバッグが得意な人は別、設計が得意な人と性能を作り込める人は同じ、という判定になる
  8. ブロック3のカテゴリを、働きかける先で分ける。これから作るもの／すでにあるもの／道具と技術の選択／一緒に働く人／相手の理解／何を作るかの決定。「ゼロから作る力」と「既存に手を入れる力」を分けるのが要点で、従来の4カテゴリ（設計・構造化、コード読解・原因究明、品質作り込み、性能・リソース）はこの2つが混ざっていたため重複が多発していた
  9. ブロック3の大分類（G1・G2・G3）を廃止する。この層の根拠は REQ-109 のレーダー3枚という表示都合だけであり、B4 にも 5.5 の判定にも使われていない。5.5 が参照しているのはカテゴリ単位である。読みやすさのためのまとまりはタグとして持ってよいが、値を集約せず、IDに含めず、5.5 から参照しない
  10. 1設問に1つの能力だけを入れる。別の能力を1問にまとめると、回答者が何をもって「できた」と言えるか判断できなくなる。同じ能力の別表現をまとめる場合は、括弧で例示を添えて曖昧さを消す
  11. ブロック3の技術系カテゴリを「設計力」と「実装力」で分ける。2026-08-15。「新規に作る力」と「既存に手を入れる力」で分ける案は採らない。読み解く力は回答者が自己評価の物差しを持っておらず、値が信用できないため。設計と実装で分けるとブロック2の工程（詳細設計・実装）と骨格が揃い、単一的ポジション（システムエンジニア、プログラマー）との対応も読み取れる。ステップアップに必要な能力が明確になる
  12. ブロック3を6カテゴリ26問とする。設計力4、実装力6、技術判断と基盤構築6、チーム運営4、伝達と説明3、業務理解と価値判断3。カテゴリあたりの設問数は揃えない（この構成は REQ-234 が5カテゴリ22問に変更した。2026-08-15）
  13. カテゴリと上位職の対応。設計力はアーキテクト、技術判断と基盤構築はテックリード、チーム運営はプロジェクトリーダーとプロジェクトマネージャー、業務理解と価値判断はシステムコンサルタントとプロダクトマネージャーの材料になる。5.5 の判定にカテゴリの平均を使うか設問を直接参照するかは未定。アクションアイテム4で扱う
  14. ブロック2を6工程23観点とする。要件定義4、基本設計4、詳細設計2、実装4、テスト4、運用・保守5。38観点から15減る。落ちたのは、全員が必ずやるため値が割れないもの（不具合の起票、単体テストの実施、新規機能の実装の細分）、工程固有でないもの（課題の特定と整理、進捗管理、問題管理）、工程ではなく企画に属するもの（システム化方針の立案）、ブロック3が持つべき能力語を含むもの（詳細設計の観点1〜4）である
  15. ブロック3の大分類を廃止した結果、汎用能力のIDから G1-1 のような記号が使えなくなる。IDの付け方は、観点の増減で過去のJSONの値が別の設問に付け替わる問題（難所6）と同じ場所にあるため、まとめて決める
- 設問案（2026-08-15 のベースライン。仕様書 v1.5 に反映済。ブロック3はその後 REQ-234 が5カテゴリ22問に変更したため、以下は v1.5 時点の記録である。現在の正は仕様書 4.4）：

  ブロック2 工程能力 23観点。アンカーは関与型。

  要件定義（4）1 顧客・利用者へのヒアリング／2 現行業務・現行システムの分析／3 要件の定義と要件定義書の作成／4 ステークホルダー間の合意形成

  基本設計（4）1 システム全体の方式・構成の設計／2 外部システムとの連携方式の設計／3 非機能要件の実現方式の設計（性能・可用性・セキュリティ）／4 基本設計書の作成と顧客への説明・承認取得

  詳細設計（2）1 担当機能の内部仕様の作成／2 設計標準・規約の策定

  実装（4）1 機能の実装（新規開発・既存改修）／2 他者のコードのレビュー／3 原因不明の不具合の切り分けと原因解析／4 性能・リソースのチューニング

  テスト（4）1 テスト計画の策定／2 テストケースの設計と実施／3 非機能テスト（性能・負荷・セキュリティ）の実施／4 受入テスト・ユーザーテストの支援

  運用・保守（5）1 運用設計・監視設計／2 日常運用・監視と障害対応／3 問い合わせ・ユーザー対応／4 変更管理・本番リリース作業／5 改修要望の受付と対応方針の整理

  ブロック3 汎用能力 26問。アンカーは習熟度型。大分類は持たない。

  設計力（4）1 オブジェクト指向・モジュール設計／2 エラー・例外設計／3 データ構造とアルゴリズムの選択／4 性能・可用性を考慮した設計

  実装力（6）1 並行・非同期処理の実装／2 メモリ・リソースの管理／3 セキュアな実装／4 既存コードの読解と不具合の原因特定／5 リファクタリング／6 性能問題の測定と改善

  技術判断と基盤構築（6）1 CI/CDの構築・運用／2 リリース方式の設計／3 選択肢の比較とトレードオフの整理／4 技術的負債の可視化と返済計画／5 規模・工数の見積り／6 新しい技術の調査と検証

  チーム運営（4）1 作業分担と進捗の管理／2 遅延・障害時の計画の組み直し／3 レビュー・ふりかえりの場の運営／4 利害が対立する場での落としどころの設計

  伝達と説明（3）1 技術文書・図解による記録／2 相手に合わせた説明（非技術者、他チーム、経営層）／3 レビューでの指摘とフィードバック

  業務理解と価値判断（3）1 業務要件から技術要件への翻訳／2 事業・ユーザー価値に基づく優先順位づけ／3 提供後の効果測定と方針の見直し

- 採って撤回した案（同じ議論を繰り返さないために残す）：
  1. 重複を4.5節に意図的なものとして登録する案。依頼者の意図と逆であり撤回
  2. 転用度・可搬性を尺度にする案。本人の能力を測るうえで不適切
  3. ブロック3の3グループの軸を維持して中身だけ入れ替える案。カテゴリ12件中2件の差し替えにとどまり、再設計になっていない
  4. テックリード用に「技術リード」カテゴリを新設する案。4観点のうち3つがアンカーAの値5の言い換えで、上記4に反する
  5. 伸びる方向（スペシャリスト／マネジメント／プロダクト）から軸を導く案。ブロック3はその人の能力を測るものであり、伸びる方向から導くものではない
  6. ブロック2の観点を「浅く調整する」だけで済ませる案。精査の結果が1観点の語尾の変更にとどまり、見直しになっていない
  7. アンカーの型を1つに統合する案。2026-08-14に反映したが同日撤回。承認を得ておらず、本REQの対象でもなかった
  8. ブロック3を16項目のフラットにし、1項目1問にする案。2026-08-14に仕様書へ反映したが、同日 依頼者の差戻しにより全面撤回。撤回の理由は、項目名が抽象的すぎて回答者が何を答えればよいか分からないこと。「構造の設計」に対してどう答えるのか、「書いて残す」とは何を指すのかが伝わらなかった。階層の廃止、全員必答の廃止、文字列ID、横棒グラフ1枚、ブロック2の33観点化も同じ案の一部であり、すべて撤回した。設問は 8fcd382 の内容に戻した
  9. 設問文に深さの語（主導、標準の策定、指導）を書かないというルール。2026-08-15に撤回。原因は設問ではなくアンカーAの文言にあった。詳細は対応方針6
- アクションアイテム：
  1. 完了。ブロック3に設問を追加しない。承認日 2026-08-15。26問のまま確定する。検討したのはテストコードの作成、育成・引き継ぎ、標準・規約の策定の3つ。育成は習熟度型アンカーの値5「人に教えられる」が全26問で同じことを聞いているため情報が重なる。標準の策定は機会が組織に左右され、能力ではなく立場が値に出る
  2. 完了。詳細設計工程の観点の見直しは REQ-233 に切り出した。2026-08-15
  3. 完了。設問のIDの付け方は REQ-231 に切り出した。2026-08-15
  4. 完了。5.5 の判定条件の書き方は REQ-216 の難所4とアクションアイテム1に統合した。数値と不可分のため単独では決められない。2026-08-15
  5. 完了。4.5 の重複表は削除した。承認日 2026-08-15。どの設問がどこと重なるかを列挙せず、ブロック2は工程で担当したかを、ブロック3は能力として使えるかを聞くという棲み分けと、集約時に足し合わせないという規則だけを残す
- 反映先：1章用語、2.5、3.2、3.4、3.5、4.2、4.3、4.4、4.5、5章出力対応表、5.3、6.1、7.1、7.2
- 状態：反映済（承認日 2026-08-15。仕様書 v1.5）

### REQ-228 課題・改善内容の元データに汎用能力が入っていない

- 出所：レビュー（2026-08-14、REQ-227の反映結果を検査する過程で判明）
- 要求：該当なし。5.6節の欠陥として起票する
- 実現性：可
- 難所：5.6節はB3「深めるべき点が分かる」の主となる出力だが、元データが「工程の重み付けと、ジャンルの重み付け」だけで、汎用能力を含まない。v1.3の時点からこうなっており、REQ-227の再設計が原因ではない。3ブロックあるうち2ブロックからしかB3を導いていない
- 対応方針：
  1. 5.6節の元データに汎用能力の項目を加える。抽出条件は現行と同じく値が2または3のもの
  2. 出力内容に汎用能力の項目名を加える。工程名・ジャンル名と並べる
  3. 汎用能力こそ打ち手が明確である。工程やジャンルの低い値は経験機会そのものを取りに行く話になるが、汎用能力の低い値は今の仕事の中で深掘りできる。5.6節の表の「打ち手：今の仕事の中で深掘りする」に最も合う
  4. 表示件数の上限は「なし」を維持する。承認日 2026-08-15。対象は値2・3に限られ、ブロック3は19問であるため、上限を設けなくても件数が発散しない
  5. 5.6節の元データの記述を実態に合わせる。「工程の重み付けと、ジャンルの重み付け」は平均を指すが、抽出条件が値2・3で出力内容が観点であるため、実際に見ているのは観点値とスロット値である。ブロック3を加えるのに合わせ、3ブロックの設問値として書き直す
  6. 出力はブロックごとに分ける。ジャンル名とスロット名、工程名と観点名、カテゴリ名と設問名の3つの並びにする。打ち手がブロックごとに違うため、混ぜて並べると読み手が区別できない
- アクションアイテム：
  1. 完了。上限は「なし」のままとする。理由は対応方針4。2026-08-15
- 反映先：5.6
- 状態：反映済（承認日 2026-08-15。仕様書 v1.13）

### REQ-229 設問構造を固着化させる記述を除去する

- 出所：レビュー（2026-08-14、ブロック3の見直し中に判明）
- 要求：仕様書とスキルに、現在の設問構造を前提とした記述が埋め込まれており、構造を見直すときに制約として働く。これを除去する
- 実現性：可
- 難所：
  1. 汎用能力の48問は「3グループ×4カテゴリ×4観点」という格子から出ている。REQ-104 の対応方針が「レーダーの軸は4から6の範囲であれば読めるため」を根拠にしており、表示の都合が設問構造を決めていた。CLAUDE.md「再設計を頼まれたとき」4に該当する
  2. doc-consistency の検査項目がカテゴリ数・グループ数を数える形になっており、構造が変わると検査自体が古い構造を強制する
  3. 検査を消すと安全網も消える。構造に依存する部分だけを外す必要がある
- 対応方針：
  1. 仕様書4.4 から格子表記（3グループ×4カテゴリ×4観点）と、レーダーの可読性を根拠にした記述を削除する。根拠は本書 REQ-104 に残るので情報は失われない
  2. 仕様書4.6 のブロック3内訳表（v1とv1.1の比較）を削除する。経緯であり執筆基準に反する
  3. 仕様書5.3 の「4カテゴリ」「4観点」という固定数を外す
  4. 仕様書2.5 と CLAUDE.md の「1カテゴリ内の観点は同質」を「同じまとまりの中の設問は同質」に改める。カテゴリという構造の存在を前提にしない
  5. doc-consistency の検査項目BとCを構造非依存にする。数える対象をカテゴリ数から設問の実数に変える
  6. 本書の REQ-104・REQ-105 は削除しない。本書は「なぜそうなっているか」を保持する場所であり、消すと同じ議論を繰り返す。決定が覆ったときは状態を失効にする
- アクションアイテム：
  1. 完了。4.6 設問数の節は復活させない。承認日 2026-08-15。数を2箇所で持つとずれるため、設問数は 4.2〜4.4 の各節の冒頭にだけ書く。実際に 6.1 のJSON例のキー数、4.6 の観点数、スキルの「設計の前提9項目」で、いずれも数のずれが発生していた
  2. 完了。4.5 の読み方リストは復活させない。承認日 2026-08-15
- 反映先：仕様書 1章用語、2.5、4.4、4.5、4.6、5.3、CLAUDE.md、.claude/skills/doc-consistency、.claude/skills/req-clarify、.claude/skills/requirement-triage、.claude/agents/req-analyst、.claude/agents/spec-reviewer
- 状態：反映済（承認日 2026-08-15。仕様書 v1.5）

### REQ-230 アンカーを習熟度型と関与型の2つに整理する

- 出所：レビュー（2026-08-15、ブロック3の設問を詰める過程で判明）
- 要求：アンカーが3種類あるが、3つとも「どう関わったか・どれだけやったか」を測っており、「どれだけ使えるか」を測る型が1つもない。ブロック1とブロック3は「できるか」を聞いているのに、関与を測る物差しを当てている
- 実現性：可
- 難所：
  1. A 自立度型とB 関与深度型はほぼ同じものを測っている。値2はどちらも補助として関わったことを指し、値3〜5も責任の重さを言い換えているだけである
  2. C 実践頻度型は上2段（定着させ他者にも広めた、常時実践している）が習熟度、下2段（複数回、一度だけ）が回数で、1つの尺度の中で軸が変わっている
  3. A の値5「標準を定め、他者を指導できる」が特定の行為を名指ししているため、「チーム標準の策定」のような設問に当てると入れ子になる。これを設問側の欠陥と誤認し、REQ-227 で設問を削除していた
  4. 現行は「型はカテゴリ単位で選ぶ」「観点ごとの文言は作らない」と定めており、設問に合わせてアンカーを決めることができない
- 対応方針：確定。承認日 2026-08-15
  1. アンカーを2種類にする。習熟度型と関与型
  2. 習熟度型の5段。1 できない／2 知識はあるが、実務では行っていない／3 やればできるが、時間がかかる／4 実務で必要に応じて、自分で行える／5 人に教えられる。より良いやり方を提案できる
  3. 関与型の5段。1 未経験／2 補助・部分的に関わった／3 担当した／4 主担当として、一人で完結させた／5 進め方や標準を定め、他者を導いた
  4. 割り当てはブロック単位とする。ブロック1とブロック3は習熟度型、ブロック2は関与型。ブロック2で習熟度型を使うと、値2「知識はあるが実務では行っていない」がその工程を担当していないことと同義になり、担当の有無がぼやける
  5. 「型はカテゴリ単位で選ぶ。同一カテゴリ内で揃っていればよい」と「観点ごとの文言は作らない」を廃止する。設問に合わせてアンカーを決める順序に改めるため
  6. 実践頻度型は採用しない。頻度はその人の力ではなく職場の事情を測る。できるのに触らせてもらえない、業務が枯れた技術ばかりで調査の機会がない、といった環境差が値に出る。ウォーターフォールとアジャイルの進め方を1問に統合したのと同じ理由による
  7. 規模の軸は採用しない。規模は能力ではなく経験した環境の属性である。またインフラ系に限らずほぼ全設問に効くため、一部の設問だけに入れることもできない。習熟度と規模を1つの物差しに混ぜると、回答者がどちらで答えるべきか判断できなくなる
  8. 上記7の根拠。本シートの目的は現在地の言語化であり、人事評価でも要員アサイン判断でもない。規模で難易度を補正したくなるのは人を比べるときであり、順位も適合度％も出さない設計では補正する必要がない。加えて REQ-004 のとおり自己評価の精度はアンカーの明確さで決まるため、軸を増やすと基準がぼやけて精度が落ちる
  9. 規模を落とすことで天井効果（小規模しか経験していない回答者も値4〜5に達する）が起きうる。運用データで確認された場合は REQ-207 の運用ルール（回答20件以上かつ値4・5が80%超で書き換えを検討）で対処する。先回りして全設問に規模を足さない
- 反映先：仕様書 3.4、4.2、4.3、4.4、7.1
- 状態：反映済（承認日 2026-08-15。仕様書 v1.5）

### REQ-231 設問のIDの付け方を決める

- 出所：レビュー（2026-08-15、ブロック3の大分類を廃止した結果として発生）
- 要求：大分類の廃止により `general.G1-1.2` の形式が使えなくなった。汎用能力のIDを決め直す。あわせてブロック2の観点IDも見直す
- 実現性：可
- 難所：
  1. 現行のIDは `process.requirements.3` のように位置番号を含む。設問を増減すると番号の意味がずれ、過去のJSONを読み込むと値が別の設問に付け替わる。6.3節のスキーマ互換性の規則では検出できない
  2. ブロック2の観点を38から23に減らしたため、この付け替わりが実際に発生する状態にある
  3. カテゴリ記号をIDに含めると、カテゴリ名を変えたときにIDが動く
- 対応方針：未定。設問がすべて確定してから決める。設問が動いている間に決めると決め直しになる
- アクションアイテム：
  1. 仕様書3.5 から汎用能力のID規則を外してある。決定後に書き戻す
  2. 仕様書6.1 のJSON例から general ブロックを外してある。決定後に書き戻す
- 反映先：仕様書 3.5、6.1、6.3
- 状態：保留

### REQ-232 汎用能力の出力形式を決める

- 出所：レビュー（2026-08-15、ブロック3の大分類を廃止した結果として発生）
- 要求：5.3 は汎用能力をG1・G2・G3のレーダー3枚で表示すると定めていたが、大分類の廃止により成立しなくなった。何で表示するかを決め直す
- 実現性：可
- 難所：
  1. 汎用能力は 2.2 の B4「分野が変わっても持っていけるものが分かる」の主となる出力である。出力をゼロにはできない
  2. レーダーの枚数と軸数が設問構造を縛った過去がある（REQ-109）。表示の都合で構造を決めないよう、構造が固まってから決める必要がある
- 対応方針：承認日 2026-08-15。依頼者の指定による
  1. カテゴリごとに1枚のレーダーを出す。軸はそのカテゴリの設問とし、軸の数はカテゴリの設問数に一致する。設計力4軸、実装力5軸、技術判断と基盤構築3軸、プロジェクト運営4軸、業務理解と価値判断3軸の5枚になる
  2. カテゴリの平均を軸にした1枚に集約する案は採らない。平均は 5.5 の判定条件が使う値であり、B4「分野が変わっても持っていけるものが分かる」は設問の単位で読むものである。平均にすると何が持っていけるのかが消える
  3. 結果画面の見出しは「汎用能力」とする。図の種類を見出しにしない。何が表示されているかは見出しで読めるべきで、レーダーチャートであることは見れば分かる
  4. 明細表は残す。レーダーは形を、明細表は値を読むためのもので、工程が両方を持っているのと同じ扱いにする
  5. 軸の数が3のカテゴリは三角形になる。軸数を揃えるために設問を足したり減らしたりはしない。表示の都合で設問構造を決めない（REQ-229）
- アクションアイテム：
  1. 完了。仕様書5.3 を汎用能力のレーダーチャートとして書き戻した。2026-08-15
  2. 完了。5章の出力対応表でB4の主となる出力をレーダーチャートに、補助を明細表にした。2026-08-15
- 反映先：仕様書 5.3、5章出力対応表、7.2
- 状態：反映済（承認日 2026-08-15。仕様書 v1.13）

### REQ-233 詳細設計工程の観点を見直す

- 出所：レビュー（2026-08-15、ブロック2を23観点に整理した過程で判明）
- 要求：詳細設計工程が2観点しかなく、工程として成立しているか確認できていない。観点の構成を見直す
- 実現性：可
- 難所：
  1. 5.2 の守備範囲マップは工程の列を「その工程の観点の平均が2以上」で有効判定する。2観点だと、内部仕様の作成に2、標準策定に1と答えた回答者は平均1.5になり列が無効になる。詳細設計に補助として参加した経験が丸ごと消える。標準を作る役割は組織が決めるため、若手ほど不利になる
  2. 「設計標準・規約の策定」は詳細設計固有の作業ではない。規約や開発標準はプロジェクトの立ち上げ時に決めるものであり、ISO/IEC 12207 でも詳細設計プロセスのタスクではなく成果物の評価基準として現れる
  3. 観点を足そうとしても候補がない。「実装者への設計説明」は設計者と実装者が別人の現場でしか発生せず、「設計変更の影響調査と設計書改訂」は運用・保守5「改修要望の受付と対応方針の整理」と重なる
  4. 詳細設計は6工程のうち唯一、工程として存在しない現場がある。IPA は応用情報技術者試験シラバス Ver.7.0 で方式設計と詳細設計の区分を廃止している。アジャイルや内製の回答者はこの工程名に心当たりがなくブロックを開かないため、未回答は値1という規則により詳細設計だけが穴になったマップが出る
- 対応方針：承認日 2026-08-15。依頼者が仕様書を直接修正した内容を確定とする。選択肢3を採り、観点を足して3観点にした
  1. 詳細設計を3観点とする。1 単一機能の内部仕様の作成／2 データ構造やアルゴリズム選定／3 ログやインターフェース部分の設計。観点1は「担当機能」を「単一機能」に改め、対象が1機能の単位であることを示した
  2. 「設計標準・規約の策定」を削除する。難所2のとおり詳細設計固有の作業ではなく、担う役割を組織が決めるため、能力ではなく配置が値に出る
  3. 上記2は REQ-227 対応方針6 が復活の対象に挙げていたが、あれはアンカーの入れ子を理由とした削除の取り消しである。工程への帰属を理由とする本件の削除は別の軸であり、取り消しの対象にはならない
  4. 難所1の列判定の歪みは、観点が3つになったことで緩和される。ただし原因は工程ごとに観点数が違うのに一律「平均2以上」で判定している 5.2 の側にあり、本REQでは解消しない。アクションアイテム1で扱う
  5. 難所3の「候補がない」は、能力語を避けるという制約下での結論だった。確定した観点2・3は主題がブロック3の設計力と重なる。4.5節の「ブロック2は工程で担当したかを、ブロック3は能力として使えるかを聞く」で切り分ける前提で採用した
  6. 難所4（詳細設計という工程名に心当たりがない回答者）は解消しない。工程名を残す判断である
  7. 選択肢4（基本設計との統合）は採らない。5工程にすると 5.5 の単一的ポジションを分ける材料が1列減る。判定条件の数値は REQ-216 で未定であり、数値を決める前に材料を減らす順序を採らない
  8. ブロック2は6工程24観点になる
- アクションアイテム：
  1. 5.2 の工程の列の有効判定を見直す。観点2つの工程と観点5つの工程を同じ「平均2以上」で判定しているため、観点の少ない工程ほど1つの回答で列が無効になりやすい。詳細設計に歪みが集中して現れていたのは観点数が最少だったためで、ルール側の非対称は残っている
  2. 観点2「データ構造やアルゴリズム選定」の語尾を検討する。「選定」はブロック3 設計力3「データ構造・アルゴリズムの選択」と同語であり、ブロック2の担当軸で書くなら別の語になる
- 反映先：仕様書 4.3.3
- 状態：反映済（承認日 2026-08-15。仕様書 v1.9）

### REQ-234 ブロック3の非技術系3カテゴリを統廃合する

- 出所：レビュー（2026-08-15）
- 要求：チーム運営・伝達と説明・業務理解と価値判断の3カテゴリは、マネジメントを不必要に分割しているように見える。統廃合して2カテゴリに集約する
- 実現性：可
- 難所：
  1. 業務理解と価値判断は REQ-215 の反映先であり、2026-08-15 に承認したばかりである。ここを畳むと承認済みの反映が消える
  2. REQ-216 のプロダクトマネージャー判定は、廃止済みIDの G3-2（チーム運営・リーダーシップ）と G3-3（顧客・ステークホルダー対応）を参照している。置き換え先が本REQの対象カテゴリにあるため、構成を変えると置き換え先も変わる
  3. カテゴリ数が変わると REQ-231（ID）と REQ-232（出力形式）の前提が動く
- 対応方針：承認日 2026-08-15
  1. 3カテゴリを2カテゴリにする。1カテゴリへの統合は採らない。プロジェクトを回す力と、作るものを決める力は、得意な人が別々になりうるため。統合するとプロジェクトリーダーとプロダクトマネージャーが同じ材料で判定されることになり、回答者に返す現在地としても「人と日程を動かす側か、作るものを決める側か」の区別が消える
  2. 「伝達と説明」を廃止し、残す設問をプロジェクト運営側へ移す
  3. 「技術文書・図解による記録」は削除する。設計書を書く力はブロック2の基本設計4と詳細設計1で測っており、ブロック3側に残るのは図解して伝えることだけで、「相手に合わせた説明」と重なる
  4. レビューに関する2設問（「レビューでの指摘とフィードバック」「レビュー・ふりかえりの場の運営」）は、統合ではなく両方とも削除する。理由は2つ。第一に、レビューで良い指摘ができることは良い設計と良いコードを知っていることとほぼ同義であり、設計力4問と実装力6問の従属変数に近い。独立して持ち込む情報が少なく、単独で伸ばす対象にもならない。第二に、レビュー会やふりかえりを主催する役目は組織が配るため、値に配置が混じる。REQ-233 の難所1 で「設計標準・規約の策定」を問題視したのと同じ構造である。担当経験はブロック2 実装2「他者のコードのレビュー」で取れる
  5. 「遅延・障害時の計画の組み直し」は単独の設問にしない。作業分担と進捗の管理を異常時という一場面に限定したものであり、障害の多い現場にいたかどうかという環境が値に出る。実践頻度型のアンカーを採らなかったのと同じ理由
  6. 「規模・工数の見積り」を技術判断と基盤構築からプロジェクト運営へ移す。見積りは技術の判断ではなく、計画を立てる行為の一部である
  7. 計画に関わる3設問（作業分担と進捗の管理、計画の立案、見積り）は、「規模・工数の見積り」と「作業計画の立案と運用（日程、順序、依存関係、分担、進捗）」の2つに集約する
  8. 立案と運用は分けない。分担を決めることも、遅れたときに組み直すことも、それ自体が計画を立てる行為であり、計画を回す人は小さい計画を常に立てている。同じ場面を経験した2人で値が割れないため、分けても2設問がほぼ同じ値になる
  9. 見積りは分ける。工数だけを問われる場面は、計画を持たない立場にも発生する。逆に計画は回すが工数は上位が出す形もあり、値が割れる
  10. 変更後のブロック3は5カテゴリ・22問とする。設計力4、実装力6、技術判断と基盤構築5、プロジェクト運営4、業務理解と価値判断3
- アクションアイテム：
  1. 完了。カテゴリ名は「プロジェクト運営」とする。承認日 2026-08-15
  2. 完了。レビュー・ふりかえりの設問は削除した。理由は対応方針4。承認日 2026-08-15
  3. 完了。仕様書 4.4 に反映した。2026-08-15
  4. REQ-216 のアクションアイテム2（G3-2・G3-3 の参照先の置き換え）に、本REQの結果を渡す
- 反映先：仕様書 4.4（4.4.3、4.4.4、旧4.4.5 を廃止、旧4.4.6 を 4.4.5 に繰り上げ）
- 状態：反映済（承認日 2026-08-15。仕様書 v1.6）

### REQ-235 ブロック3の各設問に説明文を付ける

- 出所：レビュー（2026-08-15）
- 要求：設問名だけでは何を聞かれているのか解釈が広すぎる。「セキュアな実装」が入力値の検証を指すのか例外処理の丁寧さを指すのか、「エラー・例外設計」「リファクタリング」が何を指すのか、回答者によって読みが変わる。各設問に説明文を付け、その結果として類似した設問や言葉足らずな設問が残っていないかを確認する
- 実現性：可
- 難所：
  1. 説明文に分野固有の例を書くと、その分野の回答者にしか当たらなくなる。2.5 設計ルール2 は分野名・製品名・分野固有の設計様式を禁じている。セキュアな実装の例として特定の攻撃名を挙げると、Web以外の回答者が自分の話ではないと判断する
  2. 説明文を書くと設問の範囲が確定するため、これまで名前の曖昧さで隠れていた設問どうしの重複が表に出る
  3. 説明文が長いと、回答者が読まずに設問名だけで答える。設問名の解釈が広いという元の問題が戻る
- 対応方針：
  1. 説明文は各設問1〜2文とする。「〜できる」の形で書く。アンカーが習熟度型であり、値の判定が「できるかどうか」であるため、文体を揃える
  2. 説明文は範囲を狭める目的で書く。設問名を言い換えるだけの説明は書かない
  3. 例示は分野を横断するものだけを使う。特定分野の攻撃名、製品名、設計様式名は書かない
  4. 「オブジェクト指向・モジュール設計」を「モジュール分割と依存関係の設計（オブジェクト指向、デザインパターンなど）」に改名する。承認日 2026-08-15。オブジェクト指向は特定の分野に紐づく様式ではないため 2.5 設計ルール2 には抵触しないが、この設問が測るのはパターンの知識ではなく分割と依存を決める判断であり、設計手法の名前は例示にとどめるのが正しい。オブジェクト指向だけに限定しないため、デザインパターンを併記する
  5. 「選択肢の比較とトレードオフの整理」と「新しい技術の調査と検証」を「技術選定と調査・検証」1問に統合する。承認日 2026-08-15。説明文を書いた結果、どちらも「複数の手段を評価して人に示す」になり、違いが対象の既知・未知だけになったため
  6. 「CI/CDの構築・運用」と「リリース方式の設計」は両方残す。手作業でリリースする現場では方式の設計だけが発生するため、仕組みを組む力と出し方を決める力は別々に現れる
- アクションアイテム：
  1. 完了。説明文を提示し承認を得た。2026-08-15
  2. 完了。表に出た重複と言葉足らずは対応方針4〜6 で解消した。2026-08-15
  3. 完了。仕様書 4.4 に反映した。ブロック3は 22問 → 21問。2026-08-15
- 反映先：仕様書 4.4
- 状態：反映済（承認日 2026-08-15。仕様書 v1.7）

説明文（2026-08-15。仕様書 v1.7 に反映した時点の記録。設問名と並び順はその後 REQ-236 が変更したため、現在の正は仕様書 4.4）：

設計力

| 設問 | 説明文案 |
| --- | --- |
| モジュール分割と依存関係の設計（オブジェクト指向、デザインパターンなど） | 確立された設計の型を踏まえて、機能をどの単位の部品に分け、部品どうしの依存をどう決めるかを判断できる。変更の影響が一箇所に収まる構造にできる |
| エラー・例外設計 | 異常をどこで検知し、どこで処理し、どこまで呼び出し元に伝えるかを決められる。処理を続けるか止めるか、何を記録するかの方針を立てられる |
| データ構造とアルゴリズムの選択 | 扱うデータの量と使われ方に合わせて、保持の仕方と処理の手順を選べる。計算量とメモリ使用量の見当がつく |
| 性能・可用性を考慮した設計 | 求められる応答速度や停止許容時間から逆算して構造を決められる。負荷が集中する箇所や単一障害点を設計の段階で見つけられる |

実装力

| 設問 | 説明文案 |
| --- | --- |
| 並行・非同期処理の実装 | 複数の処理を同時に動かす実装ができる。競合、順序の入れ替わり、待ち合わせの不具合を避ける手当てができる |
| メモリ・リソースの管理 | 確保したメモリ、ファイル、接続などを使い終わった時点で確実に解放できる。リークや枯渇の原因を追える |
| セキュアな実装 | 外部から入る値を信用せずに検証し、権限の確認と秘密情報の扱いを実装できる。既知の攻撃手口を踏まえて書ける |
| 既存コードの読解と不具合の原因特定 | 自分が書いていないコードを読んで処理の流れを追える。再現手順とログから原因箇所を絞り込める |
| リファクタリング | 動作を変えずにコードの構造を整理できる。どこから手を付けどこで止めるかを判断でき、壊していないことを確かめる手段を用意できる |
| 性能問題の測定と改善 | 遅い、重いという症状を計測して原因箇所を特定できる。推測ではなく計測に基づいて手を入れられる |

技術判断と基盤構築

| 設問 | 説明文案 |
| --- | --- |
| CI/CDの構築・運用 | ビルド、テスト、配置が自動で流れる仕組みを組める。失敗したときに原因が分かる形にできる |
| リリース方式の設計 | 本番環境へどう出すかを決められる。停止時間、影響範囲、切り戻しの手順を織り込める |
| 技術選定と調査・検証 | 未経験のものを含めて複数の技術を調べ、小さく試したうえで、何を得て何を失うかを整理して選べる。選んだ理由が後から追える形で残せる |
| 技術的負債の可視化と返済計画 | 今のつくりのどこが将来の足かせになるかを指摘できる。放置した場合の影響と、直す順序を示せる |

プロジェクト運営

| 設問 | 説明文案 |
| --- | --- |
| 規模・工数の見積り | 作業量を根拠のある形で見積もれる。前提とぶれ幅を添えて示せる |
| 作業計画の立案と運用 | 作業を洗い出して順序と担当を決め、日程に落とせる。進み具合を把握し、ずれたら組み直せる |
| 利害が対立する場での落としどころの設計 | 立場の違う相手の言い分を整理し、双方が受け入れられる案を作れる。決まらない場を決まる形に持っていける |
| 相手に合わせた説明 | 相手の前提知識と関心に合わせて、話の粒度と言葉を変えて説明できる |

業務理解と価値判断

| 設問 | 説明文案 |
| --- | --- |
| 業務要件から技術要件への翻訳 | 顧客や利用者がやりたいことを聞き取り、実現する仕組みの要件に置き換えられる。語られていない前提を確認できる |
| 事業・ユーザー価値に基づく優先順位づけ | やることの候補が複数あるとき、事業や利用者にとっての効果を基準に順序を決められる。やらないものを決められる |
| 提供後の効果測定と方針の見直し | 出したものが狙いどおりに使われているかを測れる。結果を見て続けるか変えるかを判断できる |

### REQ-236 ブロック3の設問名と並び順を整える

- 出所：レビュー（2026-08-15、REQ-235 の反映結果を依頼者が確認して判明）
- 要求：説明文に対して設問名が分かりにくい。並び順を回答しやすいものからにできないか。設計力の3と4、実装力のリファクタリングと性能改善、技術的負債の設問について、重複または存在意義が疑わしい
- 実現性：可
- 難所：
  1. 設問名を目的で書こうとすると抽象語になる。「データ構造・アルゴリズムの選択」を「処理効率の設計」にすると、何を聞かれているか分からなくなる。これは過去に全面差戻しになった方向である
  2. 並び順を難易度順にすると、番号の後ろほど上位という読みが生まれ、順位を出さないという設計と衝突する。難易度は運用データがないため観測もできない
- 対応方針：承認日 2026-08-15
  1. カテゴリ内は、日常業務で出会う頻度が高いものから並べる。難易度順とは呼ばない。頻度は説明できるが難易度は観測できないため。結果として回答しやすいものが先に来る
  2. 設問名は名詞句で短く書く。例示は設問名に括弧で添えず、説明文に書く
  3. 同じカテゴリの設問は、対象物が互いに重ならない名前にする。設計力1の「モジュール分割と依存関係の設計」が「設計」全般に読め、3と4を含んでいるように見えていた。「モジュール・依存関係の設計」に改め、対象物をモジュールと依存関係に限定した
  4. 設計力の3と4は統合しない。3はデータの持ち方という部品の中の判断、4はシステム構成という全体の判断であり、対象の大きさが違う。両方の説明文が効率に触れていたために境界が見えなくなっていたので、3から性能の語を外し、4に「全体の構成」を明記した
  5. 設計力3の名前は「データ構造・アルゴリズムの選択」のままとする。測りたいのは「データ量が増えても破綻しない方式を選べること」だが、それを名前にすると抽象語になる。名前は具体のまま置き、目的は説明文に持たせる
  6. 実装力の「既存コードの読解と不具合の原因特定」と「性能問題の測定と改善」を「不具合・性能問題の原因特定」1問に統合する。どちらも症状から原因箇所を絞り込む力で、違うのは症状が「動かない」か「遅い」かだけだった。統合後の1問は 2.2 の B4 が例に挙げている「デバッグ力は組込みに移っても使える」に一致する
  7. 「技術的負債の可視化と返済計画」を削除する。「どこが将来の足かせになるかを指摘できる」は設計力4問の従属変数であり、「直す順序を示せる」は業務理解2「価値に基づく優先順位づけ」の部分集合である。加えて、返済計画を作った経験の有無は職場が決める
  8. 「利害が対立する場での落としどころの設計」を「利害調整」に改める。「合意形成」はブロック2の要件定義4と同じ語になるため使わない
  9. 「業務要件から技術要件への翻訳」を「業務課題の技術要件化」に改める。要件という語が2回出て読みにくく、翻訳は比喩である。入口を課題、出口を技術要件とすることで、何から何への変換かが名前だけで読める
- アクションアイテム：
  1. 完了。仕様書 4.4 に反映した。ブロック3は 21問 → 19問。2026-08-15
- 反映先：仕様書 4.4
- 状態：反映済（承認日 2026-08-15。仕様書 v1.8）

### REQ-237 基本設計の観点の曖昧性を除く

- 出所：レビュー（2026-08-15、REQ-233 を検討する過程で判明）
- 要求：基本設計1「システム全体の方式・構成の設計」が曖昧で、何ができれば該当するのか分からない。対象の範囲も広すぎる
- 実現性：可
- 難所：
  1. ブロック2の観点は分野に依存してはならない。5.2 が「工程はジャンルに依存しない回答者の属性なので、有効な工程は列全体に色が付く」と定めているためである。具体化しようとして「画面の詳細設計」「テーブル定義」のような成果物名を置くと、その成果物を作らない分野の回答者の列が落ちる。分野はブロック1が測る
  2. 具体化のもう一方の道である判断軸（方式の決定、構成の選択）は能力語であり、ブロック3が持つ。REQ-227 対応方針14 が詳細設計の観点1〜4 を落としたのはこの理由である
  3. 上記2つにより、観点名で具体化する道が塞がっている。曖昧に見える記述が、2つの制約を同時に満たした結果である場合がある
- 対応方針：承認日 2026-08-15。依頼者が仕様書を直接修正した内容を確定とする
  1. 基本設計1を「基本的なシステムの構成設計（機能の組み合わせ）」に改める。括弧で対象を機能の組み合わせに絞り、「システム全体」という無制限のスコープを外した
  2. 技術スタックや製品の選定はこの観点に含めない。ブロック3 技術判断1「技術選定と検証」が持つ
  3. 基本設計の残り3観点は変更しない。2 は対象が外部システムに限定され、3 は括弧で3つ例示され、4 は成果物と行為が明示されており、曖昧性の問題はない
  4. 4.2 の「システムコンサルタントはこの表からは導出せず、5.5 のポジション側で扱う」の一文を削除する。5.5 が単一的ポジションと上位職の導出元を工程と汎用能力カテゴリとして定めているため、ジャンル表の側で断る必要がない
- アクションアイテム：
  1. ブロック2の各観点に説明文を付けるかを決める。ブロック3は REQ-235 で全設問に説明文を持つが、ブロック2にはない。難所1・2により観点名での具体化が塞がっている以上、曖昧性を減らす手段は説明文しか残っていない。分野依存の語は、観点にすると列を落とすが、説明文の例示であれば「どれか当てはまればよい」ため列を落とさない
  2. 4.3 に「観点は分野に依存しない語で書く」という規則を置くかを決める。現在この制約は 5.2 の理由文にしか書かれておらず、4.3 を読んで観点を足すときに参照されない
- 反映先：仕様書 4.3.2、4.2
- 状態：反映済（承認日 2026-08-15。仕様書 v1.9）

### REQ-238 ブロック2を経験の有無だけを聞くものにする

- 出所：レビュー（2026-08-15、ブロック2の曖昧性を1件ずつ確認する過程で依頼者が提起）
- 要求：ブロック2は工程を担当した経験だけを聞くものにし、技術的な中身はすべてブロック3に持たせる。観点をブロック3と突き合わせて重複を判定する作業が、観点を増減させるたびに発生している。判定そのものが起きない構造にする
- 実現性：可
- 難所：
  1. 4.3 は既に「担当したかどうかを聞く。技術の中身はブロック3が持つ」と定めている。それでも24観点のうち3つが守っていない（詳細設計2「データ構造やアルゴリズム選定」、実装3「原因不明の不具合の切り分けと原因解析」、実装4「性能・リソースのチューニング」）。工程の中で何をするかを書こうとすると技術語が出てくるためであり、ルールを足しても再発を止められない
  2. 重複判定のコストが構造的に発生する。観点を1つ足すたびにブロック3の19問と突き合わせることになる。2026-08-15 の作業では詳細設計3観点と実装2観点で実際に発生した。設問を増減させるかぎり再発する
  3. 縦にも重複がある。関与型アンカーは値2「補助・部分的に関わった」から値5「進め方や標準を定め、他者を導いた」まで、関与の深さを既に測っている。観点側に深さを表す語（策定、主導）を書くと二重取りになる。REQ-233 で削除した「設計標準・規約の策定」は、アンカーの値5と同じことを聞いていた
  4. 工程あたり1観点まで振り切ると、24観点が6観点になる。5.2 の列判定は「観点の平均が2以上」なので、観点が1つなら平均がその値そのものになり、REQ-233 難所1 の歪みが原理的に消える。一方で工程内の担当範囲の広がりが測れなくなる。要件定義でヒアリングだけの人と合意形成まで持つ人が同じ値になる
  5. 5.5 の判定条件は工程の重み付けを使う。数値は REQ-216 で未定のまま止まっており、観点数が変わると条件の意味も変わる。順序を誤ると同じ数値を二度承認することになる
  6. 6.1 の JSON は観点単位で値を保持する。観点数が変わると過去データの値が別の観点に付け替わる。REQ-231（設問のIDの付け方）と同じ場所にある問題である
- 対応方針：未定。選択肢は3つ
  1. 現行維持。24観点のまま、逸脱している3観点だけを担当軸の語に直す。重複判定は今後も発生し続ける
  2. 工程あたり1観点にする。6工程6観点。深さは関与型アンカーだけで測る。重複判定が原理的に発生しなくなり、難所4の歪みも消えるが、工程内の担当範囲の解像度を失う
  3. 中間。工程内の観点は残し、観点名を成果物と行為だけで書いて技術語を一切置かない。判定は残るが、基準が「技術語が入っていないか」の1つになり、ブロック3の19問との突き合わせが不要になる
- アクションアイテム：
  1. 選択肢2を採る場合、工程内の担当範囲の広がりを何で測るかを決める。5.5 の判定条件が工程の重み付けだけを見るなら、解像度の低下が判定にどう効くかを確認する
  2. 本REQの結論は REQ-233・REQ-237 の反映内容（詳細設計3観点、基本設計1のスコープ）を上書きしうる。ブロック2に残る曖昧性の確認は、本REQを決めてから再開する
- 反映先：仕様書 4.3。選択肢2を採る場合は 3.4、5.2、5.3、5.5、6.1 にも波及
- 状態：保留

### REQ-239 上位職の判定条件を現行構成で建て直す

- 出所：レビュー（2026-08-15。REQ-216 と REQ-214 を廃止・統合して建て替え）
- 要求：5.5 の判定条件を、現行の6工程・5カテゴリの上で組み直す。上位職にプロダクトマネージャーを含める。値は暫定で置き、sample の出力を見てバランスを調整する
- 実現性：可
- 難所：
  1. なし。廃止済みIDの参照は、設問セットを組み替えれば必ず発生するものであり、難所として扱わない。REQ-216 の要求と対応方針が旧IDの上に組まれていたため、1件ずつ差し替えるより建て替えるほうが早いというだけの話である
- 対応方針：構造のみ確定（承認日 2026-08-15）。数値と土台の割り当ては未承認のため本書に置き、仕様書には書かない
  1. **暫定の数値を仕様書に置かない。**sample の出力を見て調整することが確定している以上、調整前の数値を仕様書に載せても、読み手には確定値と区別がつかない。数値は本REQの案の欄に置き、sample の出力で調整したうえで承認を得てから 5.5 に入れる
  2. 汎用能力側の条件はカテゴリの平均値を使う。設問を直接指定する方式は1問で判定が決まるため条件が厳しく、複数設問を指定する方式は条件式が複雑になる。平均は調整が最も容易であり、5.5 の元データ欄が既に「一部の汎用能力カテゴリ」と書いていることとも合う
  3. 単一的ポジション4件はすべて工程3以上で揃える。REQ-216 対応方針1 を引き継ぐ。分析の目的がB1「経験範囲を人に説明できる」であり、即戦力の判定ではないため
  4. 上位職には土台にする単一的ポジションの名前を併記し、数値条件は上位職側だけを書く。REQ-216 対応方針2 を引き継ぐ
  5. プロジェクトマネージャーの土台はシステムエンジニアとする。REQ-216 対応方針3 を引き継ぐ。厚労省job tagがプロジェクトマネージャ（IT）の就業前の経歴をシステムエンジニアとしているため
  6. 旧IDの読み替えは次のとおり。G1-1 → 設計力、G3-2（チーム運営・リーダーシップ）→ プロジェクト運営、G3-3（顧客・ステークホルダー対応）→ 業務理解と価値判断。ただしテックリードは旧 G3-2 ではなく技術判断と基盤構築を使う。REQ-227 対応方針13 が「技術判断と基盤構築はテックリードの材料」と定めており、機械的な読み替えより現行構成の意図に合うため
  7. プロジェクトマネージャーとプロダクトマネージャーは、プロジェクト運営と業務理解と価値判断の要求水準を入れ替えた鏡像にする。前者は人と日程を動かす側、後者は作るものを決める側であり、REQ-234 対応方針1 の区別をそのまま数値に落とす
  8. システムコンサルタントはプロダクトマネージャーと同じ業務理解と価値判断を使うが、要件定義4以上を課して区別する。顧客側に立って上流を深く担当する点が違う
- 案（未承認。sample の出力で調整してから 5.5 に入れる）：

  単一的ポジション。すべて工程3以上で揃える。

  | ポジション | 条件 |
  | --- | --- |
  | システムエンジニア | 要件定義3以上、または（基本設計3以上 かつ 詳細設計3以上） |
  | プログラマー | 実装3以上 |
  | テスター・QA | テスト3以上 |
  | 運用担当 | 運用・保守3以上 |

  上位職。土台の条件に以下を上乗せする。

  | ポジション | 土台 | 上乗せする条件 |
  | --- | --- | --- |
  | システムコンサルタント | システムエンジニア | 要件定義4以上 かつ 業務理解と価値判断3以上 |
  | アーキテクト | システムエンジニア | 基本設計4以上 かつ 詳細設計4以上 かつ 設計力3以上 |
  | テックリード | プログラマー | 実装4以上 かつ 詳細設計4以上 かつ 技術判断と基盤構築3以上 |
  | プロジェクトリーダー | プログラマー | プロジェクト運営4以上 |
  | プロジェクトマネージャー | システムエンジニア | プロジェクト運営4以上 かつ 業務理解と価値判断3以上 |
  | プロダクトマネージャー | システムエンジニア | 業務理解と価値判断4以上 かつ プロジェクト運営3以上 |

- アクションアイテム：
  1. sample_v2.html に値を入れて出力を見ながら上記の案を調整し、承認を得て 5.5 に入れる。全ポジションが点灯する入力や、1つも点灯しない入力が出ないかを確認する
  2. 上位職が土台の条件を満たさない場合の表示を決める。土台なしで上位職だけが点灯する経路は現在ない
- 反映先：5.5
- 状態：保留（構造は 2026-08-15 に仕様書 v1.11 へ反映済。数値と土台の割り当ては未承認。prototype/sample_v2.html が調整の材料を出力する）

### REQ-240 関連職種に未回答のジャンルが出る

- 出所：prototype/sample_v2.html にサンプル値を流して判明（2026-08-15）
- 要求：該当なし。5.4節の欠陥として起票する
- 実現性：可
- 難所：
  1. 4ジャンルに回答した状態で分析したところ、関連職種に「アプリケーションエンジニア（デスクトップ）」と「モバイルアプリエンジニア」が出た。どちらも全スロット未回答で重み付け1.00である。5.4 の出力条件は「同グループに3.5以上があり、自身が3.5未満」であり、値1のジャンルはこの条件を必ず満たす
  2. REQ-209 が定めた 5.4 の意図は「近いがまだ届いていない職種」を出すことだった。一度も触っていない分野が「近い」と言えるかは別の判断である
  3. 5.1 は値1を「課題にしない。未経験を『できていないこと』として並べても打ち手にならない」として課題・改善内容から除外している。同じ値1が 5.4 では職種名として出る。5.1 と 5.4 で値1の扱いが割れている
  4. 表示件数の上限は「なし」である。18ジャンル中14が未回答という状態は普通に起こるため、同グループに1つでも3.5以上があると、そのグループの未回答ジャンルがすべて出る
  5. 下限を設けると、グループ内に有効なジャンルが1つしかない回答者には関連職種が1件も出なくなる。出ないこと自体は誤りではないが、B2「隣の領域が見える」が空になる
  6. グループが「近さ」を表していない。4.2 の表で、Webフロントエンド（JS/TS、React、コンポーネント設計、ブラウザ互換）とデスクトップアプリ（C++/C#、WPF、MVVM、インストーラ）は4スロットすべてが別物である。同じ「アプリケーション開発」の見出しの下に並んでいるだけで、経験が通用する関係ではない
  7. sample_v1 は 5.4 に相当する出力をグループから作っていない。工程の値と回答したジャンルの値から職種名を引いており、到達済みのジャンルに職種名を付けていた。グループを使っていたのは守備範囲マップの行の隣接だけである
  8. 職種ラベルの向きが逆になっている。旧5.7 の職種名は 5.4 からしか参照されず、5.4 は未到達のジャンルにしか出さない。回答したジャンルの職種名はどこにも出力されない。B1 の例文は「Webバックエンドで要件定義から運用まで担当してきた」だが、バックエンドエンジニアという名前が出る経路がない。REQ-209 は「到達済みは 5.5 が担う」を前提にしたが、5.5 は工程から算出するポジションであり、ジャンル側の職種名を受け取っていない
- 対応方針：
  1. 起票時の3案（現行維持・下限を設ける・出力を分ける）はいずれも採らない。3案とも閾値と値の扱いの調整であり、グループを近さの代わりに使っている点に触れていない。欠陥は閾値ではなく、近さを定義するデータが存在しないことである
  2. ジャンルの関連表を新設し、近さを一次データとして持つ。グループは表の見出しに戻し、分析には使わない
  3. 関連の根拠は、同じ案件の中で隣り合って現れることとする。技術（S1〜S4）の重なりは根拠にしない。関連するジャンルは主戦場と異なる技術を要するため、スロットが一致しないことは関連を否定しない。むしろスロットが重なるほど同じ業務に近づき、別のジャンルとして分けている意味が薄れる。この判断は依頼者による（2026-08-15）
  4. 上記3に至る前に、技術の共有（2スロット以上で経験が通用する）を根拠にする案を組んだが撤回した。この案ではWebフロントエンドとWebバックエンドが4スロットすべて別物となり、関連しないことになる。関連するとしか読めないペアが落ちる時点で、根拠の取り方が誤っていた
  5. 関連に方向は持たせない。方向を付けると、どちらが上位かという含みが出る
  6. 31ペアを表に置く。デスクトップアプリとモバイルアプリは、同じ案件の中で隣り合うとは言えないため関連にしない。ペアの採否は依頼者の承認による（2026-08-15）
  7. 5.4 を「職種名」に改め、守備範囲の職種（B1）と関連職種（B2）の2つを出す。難所8 を解消し、旧5.7の対応表が到達済みのジャンルにも使われるようになる
  8. 未回答のジャンルが関連職種に出ること自体は残す。関連表で結ばれていることが根拠になり、出る理由を説明できるようになったため。重み付け2以上という下限は設けない。設けると難所5 が残る
  9. 行の隣接も同じ関連表を使う（REQ-241）。5.2 は重み付け2以上を起点に接触を示し、5.4 は3.5以上を起点に職種名を出す。起点の水準を変えることで役割を分ける
  10. 規則を対応方針3 に変えたことで新たに条件を満たした5ペアを追加した。Webバックエンド・業務アプリ × 情報セキュリティ、モバイルアプリ × QA・テスト自動化、クラウド・IaC・SRE × QA・テスト自動化、通信プロトコル実装 × 情報セキュリティ、DB・データ基盤 × パッケージ・ERP・SaaS導入。Webバックエンド・業務アプリ × OS・サーバ・ミドルウェアも候補に挙げたが、採らなかった
- アクションアイテム：
  1. 運用データが蓄積した時点で、実際の共起から関連表を見直す。REQ-203 が「運用データの共起で再編する」としており、現在の31ペアはその代わりに置いた判断である。これは運用開始後の話であり、sample の出力では判断できない
- 反映先：5.1、5.2、5.4、5.7、7.2
- 状態：反映済（承認日 2026-08-15）。仕様書 v1.12

### REQ-241 守備範囲マップの行の隣接が表の並び順に依存する

- 出所：prototype/sample_v2.html にサンプル値を流して判明（2026-08-15）
- 要求：該当なし。5.2節の欠陥として起票する
- 実現性：可
- 難所：
  1. 5.2 は行の隣接を「同じグループ内で有効なジャンルに隣り合う無効なジャンル」と定める。この定義は一列の並び順を前提にしている
  2. 4.2 は「ジャンルに一列の順序は与えない。ただしグループ単位の近さは持たせる」と定める。順序を与えないと宣言したものに、順序に依存する判定を載せている
  3. 実行結果で矛盾が表に出た。アプリケーション開発グループでWebフロントエンドとWebバックエンドが有効、デスクトップアプリとモバイルアプリが未回答のとき、デスクトップアプリは隣接になりモバイルアプリはならない。どちらも同じグループの未回答ジャンルであり、扱いが分かれる理由は表の並び順しかない
  4. 列（工程）の隣接には同じ問題がない。工程には順序があると 4.3 が明記しており、隣り合うことに意味があるため
- 対応方針：
  1. 起票時の3案はいずれも採らない。1案はグループを近さとして使い続けるため REQ-240 難所6 が残り、2案は 4.2 の「順序を与えない」を覆し、3案は行の隣接という出力そのものを失う
  2. 行の隣接を「有効なジャンルとジャンルの関連表で結ばれた無効なジャンル」に変える。REQ-240 で新設した関連表をそのまま使う。表の並び順にも、グループにも依存しなくなる
  3. 列の隣接は現行のまま、工程の順序を使う。4.3 が工程に順序があると定めており、隣り合うことに意味があるため。行と列で判定の元が違う理由を 5.2 に1行書く
  4. 5.4 の関連職種とは起点の水準で役割を分ける。5.2 は重み付け2以上のジャンルを起点に接触を示し、5.4 は3.5以上のジャンルを起点に職種名を出す。使う関連表は同じものにする
- 補足：REQ-240 と同時に対応した。5.4 の関連職種と 5.2 の行の隣接は、どちらも近さの定義を必要としており、片方だけを直すと代用が残る
- 反映先：仕様書 5.2、5.7
- 状態：反映済（承認日 2026-08-15）。仕様書 v1.12

### REQ-242 レーダーチャートの対象をブロック3だけにする

- 出所：prototype/sample_v2.html の出力を依頼者が確認して（2026-08-15）
- 要求：レーダーチャートで出すのはブロック3の内容とする
- 実現性：可
- 難所：
  1. 業務ジャンル別のレーダーを廃止すると、S1〜S4 のスロット値がどこにも出なくなる。守備範囲マップの行バーは4スロットの平均であり、内訳は読めない
  2. 工程集約のレーダーを廃止しても工程の値は残る。守備範囲マップの列バーが重み付けを、明細表が観点値を持つ
  3. 5.3 は工程集約と業務ジャンル別を「B1の内訳」と位置づけていたが、5章の出力対応表はB1の補助にレーダーを挙げていない。表と本文で位置づけが揃っていなかった
- 対応方針：承認日 2026-08-15
  1. 工程集約1枚と業務ジャンル別n枚のレーダーを廃止する。レーダーは 5.3 の汎用能力だけが持つ（REQ-232）
  2. 明細表に業務ジャンル別のスロット値を加える。難所1 による。廃止によって出力から消える値をなくす
  3. 4.2 の「ジャンル間でレーダーを比較できる」を「ジャンル間で値を比較できる」に改める。4スロットが共通であることの意味は残り、レーダーで比較するという手段だけが消える
  4. 5章の出力対応表は変えない。難所3 のとおりB1の補助にレーダーを挙げていなかったため、廃止しても表は動かない
- 反映先：仕様書 4.2、5.3、7.2
- 状態：反映済（承認日 2026-08-15。仕様書 v1.13）

### REQ-243 ジャンル表の職種名に役割の名前が混じっている

- 出所：prototype/sample_v2.html の出力を依頼者が確認して（2026-08-15）
- 要求：関連職種に「業務システム導入コンサルタント」が出るのはポジションの話ではないか
- 実現性：可
- 難所：
  1. 出力の経路は仕様どおりで、算出ロジックに誤りはない。パッケージ・ERP・SaaS導入 は 5.7 の関連表で Webバックエンド・業務アプリ および DB・データ基盤 と結ばれており、自身の重み付けが3.5未満であれば 5.4 の関連職種に出る
  2. 旧5.7の18件のうち #16 だけが役割の名前で、残り17件は分野の名前である。5.4 は「工程から算出する役割は対応可能なポジションが扱う。本節はジャンルから算出する分野の名前だけを扱う」と定めており、#16 だけがこの記述から外れている
  3. 5.5 の上位職に「システムコンサルタント」がある。同じ語が2つの節から出るため、結果画面ではどちらの話か読み分けられない
  4. 旧5.7 と REQ-206 は職種名を表示ラベルと位置づけ、実務の呼称と異なる場合は表記のみ更新するとしている。呼称の当否は依頼者の判断に属する
- 対応方針：承認日 2026-08-15
  1. 職種名は、旧5.7の対応表を廃止し、4.2のジャンル定義表に統合する。ジャンルは設問・分析の単位、職種名は分析結果の表示ラベルという区別は維持する
  2. #16の職種名を「業務システム導入コンサルタント」から「業務システム導入エンジニア」へ改める。5.5の「システムコンサルタント」との役割名の衝突を解消する
  3. #16以外の職種名は現行のままとする。QA・テスト自動化は、実務上それぞれの専門性を示すため「QAエンジニア、テスト自動化エンジニア」の併記を維持する
- 反映先：仕様書 4.2、5.4、5.7（旧5.7を廃止し、旧5.8を5.7へ繰り上げ）
- 状態：反映済（承認日 2026-08-15。仕様書 v1.14）

### REQ-244 仕様書 v1.13 に合わせて sample_v3 を作る

- 出所：依頼者の指定（2026-08-15）
- 要求：prototype/sample_v2.html をコピーした v3 を作り、更新した仕様に追随させる
- 実現性：可
- 難所：
  1. v2 は仕様書 v1.12 準拠で作られており、レーダーの構成（REQ-232・REQ-242）と課題・改善内容の元データ（REQ-228）が変わる
  2. REQ-243で職種名を4.2へ統合し、#16を「業務システム導入エンジニア」に変更した。sample_v3はこのラベルに追随する必要がある
- 対応方針：
  1. sample_v2.html は残し、prototype/sample_v3.html として作る。REQ-225 の対応方針1 と同じ扱いにする
  2. 収録範囲、CDN非依存、インラインSVG、暫定の設問IDは REQ-225 の対応方針を引き継ぐ
  3. 5.5 は判定条件が未定のため出力せず、調整の材料を表示する扱いも引き継ぐ
- 反映先：prototype/sample_v3.html。仕様書への反映はなし
- 状態：未着手
